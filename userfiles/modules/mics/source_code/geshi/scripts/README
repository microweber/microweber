This directory used to contain a script that provided a way to get keywords for any
particular language. That script has now been removed.

I hope to provide different scripts in here at some point. Ideas include:

  * Make a language file, via a web interface
  * Validate a language file
    - there's already a bug for validating that the correct theme data is supplied
