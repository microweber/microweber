$('[data-toggle="filter-buttons"] .btn').on('click', function () {
    /*// toggle style
    $(this).toggleClass('active');

    // toggle checkbox
    var $chk = $(this).find('[type=checkbox]');
    $chk.prop('checked',!$chk.prop('checked'));*/

  //  return false;
});
