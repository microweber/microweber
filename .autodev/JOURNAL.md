# JOURNAL.md — Agent Research & Decision Log

Each row records one hypothesis and its outcome.

- **Status:** `keep` | `discard` | `partial` | `pending`
- **Complexity delta (ΔC):** `+` more complex | `-` simpler | `=` neutral

Never delete rows. Discards are valuable data — they prevent re-trying failed approaches.

| #  | Date       | Task                                    | Hypothesis                                                                                                          | Approach                                                                                          | Outcome                                                                            | Status | ΔC | Notes |
|----|------------|-----------------------------------------|---------------------------------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------|--------|----|-------|
| 1  | 2026-05-13 | AI-307 picker search (task-5f1937)      | An Alpine `x-data="{ q: '' }"` + per-card `x-show` with `@js($haystack)` will deliver search filtering without Livewire round-trip. | Added input + x-show gates on each card in add-content-modal.blade.php.                            | All 7 contract tests pass; search filters cards in real time.                       | keep   | =  | First-class Filament+Livewire+Alpine integration pattern; viable for other picker surfaces. |
| 2  | 2026-05-13 | AI-307 polish empty-state flash         | `x-cloak` will suppress pre-Alpine paint on the empty-state element.                                                | Initially added `x-cloak` to the empty-state element.                                              | Empty-state visibly flashed `""` literal quotes — project has no global `[x-cloak] { display: none }` rule. | discard | =  | Lesson: cannot rely on `x-cloak` without confirming the global CSS rule exists. Better: inline `style="display: none;"` default. |
| 3  | 2026-05-13 | AI-307 polish empty-state copy          | Replace query-echoing copy with a static "No content types found." + inline `style="display: none"` default.        | Two-line edit on add-content-modal.blade.php.                                                       | No flash; test pinned both shape changes.                                           | keep   | -  | Belt-and-braces: works whether [x-cloak] is defined or not. Simplification win. |
| 4  | 2026-05-13 | AI-269+272 DashboardEmptyStateWidget    | Filament widget with `canView()` gated on `DB::table('content')->count() === 0` cached 60s will appear ONLY on fresh installs. | New widget class + blade + SCSS section in microweber-theme-v3.scss.                                | All 8 contract tests pass; widget surfaces only when content is empty.               | keep   | +  | New widget infra worth the complexity given multi-AC coverage. Sort -1 between welcome (-2) and stats (0). |
| 5  | 2026-05-13 | AI-274/268 UndoRedo wrapper visibility  | Removing `hidden` class from `.live-edit-undo-redo-buttons-wrapper` in Toolbar.vue will surface the existing UndoRedo buttons. | Single-line class removal + title attributes + Ctrl+Z/Y/Shift+Z bindings.                          | All 7 contract tests pass; visible buttons + working shortcuts.                     | keep   | -  | Pure visibility unblocking — affordance was 100% there in code, just hidden by CSS. |
| 6  | 2026-05-13 | AI-246 admin table card-view <1024px    | Single `@media (max-width: 1023.98px)` CSS block scoped to `body.fi-panel-admin` will flip every admin table to a stacked-card layout without DOM/PHP changes. | CSS-only addition to mobile-touch.css; thead hidden, .fi-ta-row → flex-column card, .fi-ta-cell → 100% width + flex-wrap. | All 7 contract tests pass; built bundle verified.                                   | keep   | =  | Filament's consistent `.fi-ta-*` DOM means one CSS block covers EVERY resource list automatically. Highest leverage:effort ratio of the day. |
| 7  | 2026-05-13 | AI-246 heredoc append to mobile-touch.css | Bash heredoc append should add the AI-246 block to the end of mobile-touch.css.                                  | `cat >> file <<'EOF' ... EOF` heredoc append.                                                       | File ended up with DUPLICATED mid-stream content (line 781 mid-rule + full second copy from line 788). Build failed. | discard | =  | Lesson saved to memory feedback_heredoc_append.md: use Edit tool with old_string=existing-tail / new_string=existing-tail+new-block for appends. Pattern duplication is reproducible. |
| 8  | 2026-05-13 | AI-240 mw-modal focus management        | Vanilla JS focus trap + Escape handler + symmetric open/close via existing `activeModalComponentChanged`/`closeModal` hooks will add WCAG 2.1.1/2.1.2/2.4.3 without a new npm dependency. | mwTrapFocusForModal/mwReleaseFocusForModal + mwModalKeydownHandler with focus-stack.                | All 7 contract tests pass; nested-modal safety pinned by stack guard.                | keep   | +  | Filament's `.fi-modal` already has Alpine x-trap; this slice covers ONLY the bespoke mw-modal layer. |
| 9  | 2026-05-13 | AI-210 product-title color via token    | Rewiring `--color-product-title` to `var(--color-text-primary)` + adding a `.shop-products .product > a` !important override will remove Big2's hardcoded `#f4a261` orange without per-template work. | Token rewire in design-system.css + mirror in public-touch.css + product card override + served mirror sync. | All 10 contract tests pass.                                                          | keep   | -  | Token chaining wins again — one canonical value, every consumer follows. |
| 10 | 2026-05-13 | AI-209 MwColors::Blue re-anchor         | Re-anchoring MwColors::Blue[500] to `(13, 110, 253)` (Bootstrap #0d6efd) + switching FilamentCheckoutPanelProvider from Color::Blue to MwColors::Blue will unify admin + checkout + public primary blue. | RGB rewrite of all 11 weights in MwColors.php + import + colors() swap in checkout provider.       | All 16 contract tests pass (11 via DataProvider across the ladder).                  | keep   | =  | Three-surface unification via one constant + one provider line. |
| 11 | 2026-05-13 | AI-211 checkout form radius             | `body.fi-panel-checkout`-scoped override pulling every input/wrapper/button to `var(--radius-sm, 4px) !important` will unify the checkout form palette without touching the admin 7px brand. | Single CSS block appended via Edit tool (avoiding the heredoc footgun from #7).                   | All 11 contract tests pass; admin 7px untouched per regression guard.                 | keep   | =  | The Edit-tool append pattern (per #7's lesson) worked cleanly. |
| 12 | 2026-05-13 | AI-293 RTL first slice (AC #4 only)     | The HTML root already carries `dir="rtl"` via `lang_attributes()` — so a `[dir="rtl"]` CSS selector list flipping directional Heroicons + Filament chevron pseudo-elements will deliver AC #4 without ANY blade/PHP changes. | CSS-only addition to mobile-touch.css. 26 Heroicon selectors + 3 pseudo-elements via `transform: scaleX(-1)`. | All 28 contract tests pass (17 directional icons via DataProvider, 7 non-directional regression guards). | keep   | =  | Bounded slicing strategy pays off — 1 of 7 ACs shipped cleanly, the other 6 are now well-scoped follow-ups. Won't ship the rest without test content + explicit dispatch. |
| 13 | 2026-05-13 | AI-293b text-align: start sweep (task-46e417) | Sweeping 38 `text-align: left/right` + 21 `margin-left/right` rules to logical equivalents will satisfy AI-293 AC #2/#3. | Considered but did NOT execute.                                                                  | Sweep without RTL test content risks LTR regressions on intentional left-aligned elements (data columns, labels, mixed-mode contexts). | discard | =  | Lesson: large mechanical refactors need test content or explicit dispatch. Deferred until [DISPATCH] AI-293b arrives. |
| 14 | 2026-05-16 | task-cfef17 dark/light back to submenu | Removing the AI-168 `TOPBAR_END` render-hook injection of `<x-filament-panels::theme-switcher />` will restore the Filament-stock location for the switcher (inside the user-menu dropdown), which is what the user means by "as they were before". The original AI-168 worry that the user-menu version "isn't visible" was either transient or the user has decided the user-menu submenu is now the correct home. | Single-block deletion of the `FilamentView::registerRenderHook(PanelsRenderHook::TOPBAR_END, ...)` call + its docblock in `packages/microweber-filament-theme/src/MicroweberFilamentTheme.php`. | Topbar no longer renders the 3 theme-switcher icons inline; Filament's stock user-menu dropdown renders the switcher per `vendor/filament/.../user-menu.blade.php` lines 112-116. | keep | - | Code-simplification win (removed 25 lines of code + docblock). The user-menu location is Filament's default + matches the user's "as they were before" intent. Bundle rebuilt via `cd packages/microweber-filament-theme && npm run build`. |

## Auto-learn 2026-05-13

Patterns extracted from rows 1-13:

- **Bounded slice + contract test is the dominant winning pattern.** Every `keep` row is a single-file or single-rule change with a focused PHPUnit contract test. The two `discard` rows (#2 x-cloak reliance, #7 heredoc duplication) both failed because they relied on assumed infrastructure.
- **Token chaining (`var(--x) → var(--y)`) consistently delivers cross-surface unification with one edit.** See rows 9 (footer/product-title), 10 (admin/checkout/public blue).
- **Filament's consistent `.fi-*` class emission is a leverage point.** One CSS block scoped to `body.fi-panel-admin` covers every resource list (row 6).
- **The `Edit` tool beats Bash heredoc for appending to existing source files.** Row 7's discard locked this in.
- **Speculative refactors deferred when no explicit dispatch + no test content** (row 13). Respect PM's queue model; flag concerns; don't manufacture work.

## Auto-learn 2026-05-13 (review #2)

Reviewed: zero new journal rows since the prior `## Auto-learn 2026-05-13` heading. All 13 existing rows were already mined by review #1; no fresh `keep` / `discard` / `partial` outcomes have been logged. Tally: 0 unreviewed entries → 0 new recurring anti-patterns, 0 new best-practice approaches, 0 new simplification wins, 0 contradicted-hypothesis knowledge gaps. No additions made to `LESSONS.md` (the seven existing entries already capture every pattern derivable from rows 1–13 plus three session-derived facts). No new skill files created.

**Meta-observation worth recording for the next review:** A large amount of session work has shipped since the last review (AI-322 / AI-323 module docs, the Live Edit upload-modal rewrite under task-f18a79, and the full NOVICE-customer audit batch #1–#15) without being logged as individual journal rows. Going forward, each non-trivial shipped task should add a `pending` row before code touches and update to `keep` / `discard` after — otherwise the auto-learn signal weakens. This gap is acknowledged here rather than backfilled retroactively, because fabricating after-the-fact rows would dilute the dataset's honesty.

## Auto-learn 2026-05-14 (review #3)

Reviewed: zero new journal rows since the `## Auto-learn 2026-05-13 (review #2)` heading. Same honest tally as review #2 — 0 new recurring anti-patterns, 0 new best-practice approaches, 0 new simplification wins, 0 contradicted-hypothesis knowledge gaps derivable from journal rows alone. The pattern flagged in review #2 (large shipped work not journaled as rows) has continued: 9 tier-1 narrative-docs commits (AI-322..AI-325, AI-331..AI-335), the AI-326 docs-hardening sweep, 6 Phase 1 UX commits (AI-510..AI-515), the AI-333 sitemap cache-TTL fix, 4 redispatch enhancement commits, and the tier-1 docs landing-page artifact all shipped without `pending → keep/discard` rows. Per review #2's anti-fabrication rule, no retroactive rows are written.

**LESSONS.md updates this cycle (5 new entries, surfaced from session work rather than journal rows):** (a) VitePress `{{ ... }}` in markdown prose breaks SSR; escape with `&#123;&#123;`; (b) `cd packages/<pkg>` leaves CWD in subdir and breaks subsequent repo-root git commands; (c) `ignoreDeadLinks` regex must accept the `./` prefix from nested pages (no `^` anchor); (d) contract-test regexes matching JS function bodies need non-greedy `.*?` not `[^)]+`; (e) cache-staleness methods must never short-circuit with unconditional returns (sitemap `return true;` incident). All five are recorded with prevention rules and "applies when" clauses. No skill files created — none of the patterns hit the 3+ recurrence threshold yet.

**Process drift to fix:** the dispatch-driven autonomous loop generates commit messages, SOUL.md threads, and TODO.md state transitions but bypasses the journal pre-task hypothesis step. The journal-as-truth signal is weakening relative to session reality. Either tighten the loop to require a journal row per dispatched task, or formally retire the per-row protocol for dispatched work and rely on SOUL.md threads + LESSONS.md for the keep/discard data. Flagging here rather than deciding unilaterally.

## Auto-learn 2026-05-14 (review #4)

Reviewed: zero new journal rows since the `## Auto-learn 2026-05-14 (review #3)` heading. The journal still terminates at row 13. The pattern flagged in reviews #2 and #3 (dispatched session work shipping without journal rows) has persisted through another arc: AI-516, AI-517, AI-518, AI-519 (commerce-mobile bounded-slice ships — commits `3157870616`, `81ffb093fb`, `a9f8dd7a1c`, `a188cdcbfe`), the maintenance trio (skill-files sync, SUMMARY.md update, LESSONS.md update), and SOUL threads #14 through #45 all shipped without `pending → keep/discard` rows. Per the anti-fabrication rule from review #2, no retroactive rows are written.

**Tally:** 0 new recurring anti-patterns, 0 new best-practice approaches from journal data, 0 new simplification wins, 0 contradicted-hypothesis knowledge gaps derivable from journal rows alone.

**LESSONS.md updates this cycle (3 new entries, surfaced from session work rather than journal rows, added in the maintenance-cycle task that immediately preceded this review):** (a) Filament `Action::color('success')` size=lg requires `!important` to enforce `min-height` because inline styles beat normal selectors (AI-517 discovery); (b) Contract-test source slices must bound to the rule's closing brace, not EOF — otherwise absence-assertions false-fail as the file grows below the marker (AI-518 `contactUsBlock()` false-fail); (c) Contract tests that grep for selectors must avoid quoting the selector in their own comments — guard assertions self-match across the test file's own prose (AI-517 guard self-match risk). None of these patterns crossed the 3+ recurrence threshold yet, so no skill files were created.

**Decision recommendation (forward-looking, not unilateral):** the journal-vs-reality drift is now a four-cycle pattern. Three options remain on the table: (1) tighten the dispatch loop to require a `pending` journal row per ticket before the `[~]` mark; (2) formally retire the per-row protocol for dispatched work and treat SOUL.md threads + LESSONS.md as the canonical keep/discard signal; (3) status quo + biweekly bulk-import from SOUL.md into journal rows (but this re-introduces the fabrication risk reviews #2 and #3 explicitly rejected). Option 2 is the leading recommendation given the natural information density of SOUL.md threads and the cost of option 1 at the per-dispatch level. Flagging for the human / PM to pick — agent will not change the protocol unilaterally.

## Auto-learn 2026-05-16 (review #5)

Reviewed: **one** new journal row since `## Auto-learn 2026-05-14 (review #4)` — row 14 (task-cfef17, dated 2026-05-16). First non-zero unreviewed-row count since review #1, suggesting a tentative breakout from the four-cycle journal-vs-reality drift flagged in reviews #2–#4 (although n=1 is not yet a trend). **Tally:** 0 new discard-recurrence anti-patterns (row 14 is `keep`, no new discards); 0 approach-recurrence best-practices (row 14's "remove agent customization to restore framework defaults" approach is novel for this dataset — not 2+ recurrence yet); **1 simplification win** (row 14 ΔC = `-`, removed 25 lines of render-hook code + docblock); 0 contradicted-hypothesis knowledge gaps (outcome matched hypothesis precisely). **LESSONS.md updates this cycle (1 new entry):** "*As they were before* feedback usually means delete prior customization, not add more" — codifies the subtractive-fix heuristic for UI-restoration tasks, prevention rule directs the agent to consult `git log --oneline -- <file>` + `git blame` before reaching for compensating CSS/JS. No skill file created — single occurrence, well below the 3+ recurrence threshold. **Process note:** the journal protocol revival in row 14 was authored as part of the task-cfef17 commit narrative rather than as a pre-task `pending` row, so the loop-tightening from review #4's Option 1 has not been adopted. Continuing to flag the protocol-choice for human decision rather than changing it unilaterally.

## Auto-learn 2026-05-16 (review #6)

Reviewed: zero new journal rows since `## Auto-learn 2026-05-16 (review #5)` — the journal still terminates at row 14. Row-14's tentative breakout from the four-cycle journal-vs-reality drift (reviews #2–#4) did not become a trend; the dispatched session work that followed (CLAUDE.md/AGENTS.md/copilot-instructions.md sync, SUMMARY.md comprehensive review, LESSONS.md +5 new entries covering this session's HTML-attribute-quoting + Tailwind v4 translate + marker-carry-forward + regex parens-crossing + dead-`!important` lessons) all shipped without `pending → keep/discard` journal rows. Per the anti-fabrication rule from review #2, no retroactive rows are written. **Tally:** 0 new recurring anti-patterns, 0 new best-practice approaches, 0 new simplification wins, 0 contradicted-hypothesis knowledge gaps derivable from journal rows alone. **LESSONS.md updates this cycle (0 from journal rows):** the 5 lessons added in the immediately preceding maintenance task (HTML attribute parser `/* */` quote-termination; Tailwind v4 dual-property `translate`/`transform`; refactor-time task-id marker carry-forward; contract-test regex `[^)]+` cannot cross lambda parens; `!important` against non-loaded stylesheets is dead code) were surfaced from session SOUL.md threads, not journal data. None crossed the 3+ recurrence threshold for skill-file promotion. **Pattern observation:** Option 2 from review #4 (formally treat SOUL.md threads + LESSONS.md as canonical keep/discard signal, retire the per-row protocol for dispatched work) is now the de-facto operating mode for the sixth consecutive cycle — the protocol choice has been made by emergent practice rather than by explicit human dispatch. Continuing to flag rather than formalise unilaterally.

## Auto-learn 2026-05-16 (review #7)

Reviewed: zero new journal rows since `## Auto-learn 2026-05-16 (review #6)` — the journal still terminates at row 14. **Tally:** 0 new discard-recurrence anti-patterns; 0 approach-recurrence best-practices; 0 simplification wins (ΔC = `-`); 0 contradicted-hypothesis knowledge gaps derivable from journal rows alone. Per the anti-fabrication rule from review #2, no retroactive rows are written. **LESSONS.md updates this cycle (0 from journal rows; 7 from session work in the preceding maintenance pass):** lessons surfaced from the AI-697..AI-702 chrome arc + ESE Phase 1 slice 1.2/1.3a/1.3b + Add-Content modal slices — Filament render-hook ordering (first-registered renders leftmost; assert via `strpos`); Vue popover dismiss handlers must use capture phase (`addEventListener('click', handler, true)`); Teleport-to-body / Filament-portaled DOM requires `:root`-scoped tokens with literal fallbacks; Vue `:class` + `class` split for state-class triads (don't combine static + dynamic); sub-slicing pattern `[ticket]a/b/c` for multi-item tickets with hard dependencies; single-`:root`-token cascade with negative-lookbehind regex pinning; single-source-of-truth grep before adding a new state indicator. None crossed the 3+ recurrence threshold for skill-file promotion; the Vue-popover capture-phase rule and the Teleport-token-scope rule are the strongest skill-promotion candidates if they recur on the next chrome ticket. **Pattern observation:** Option 2 (SOUL.md + LESSONS.md as canonical keep/discard signal, per-row protocol retired for dispatched work) is now the de-facto operating mode for the **seventh** consecutive review cycle. The session shipped a 7-ticket chrome arc (AI-697..AI-702 + AI-708/AI-709), an ESE Phase 1 slice 1.3b, a v2-inspired admin shell start (AI-702 brand mark), and three documentation maintenance passes — significant productivity, zero journal rows. Final flag-not-formalise: the per-row protocol has now been *emergently retired* for 7 review cycles without an explicit human decision; if the human wants to formalise the choice (or revive the per-row discipline), the SOUL row pattern would be the natural place to record it. Continuing as-is until then.

## Auto-learn 2026-05-17 (review #8)

Reviewed: zero new journal rows since `## Auto-learn 2026-05-16 (review #7)` — the journal still terminates at row 14. Eighth consecutive review-cycle with no new `pending → keep/discard` rows from session work. **Tally:** 0 new discard-recurrence anti-patterns; 0 approach-recurrence best-practices; 0 simplification wins (ΔC = `-`); 0 contradicted-hypothesis knowledge gaps derivable from journal rows alone. Per the anti-fabrication rule from review #2, no retroactive rows are written. **LESSONS.md updates this cycle (0 from journal rows; 3 from session work in the immediately preceding maintenance pass):** lessons surfaced from the 2026-05-17 image picker arc (AI-768 / AI-769 / AI-770 v2 / AI-771 / AI-772) — `mw.tabs()` `trigger("click")` on an already-active tab hits the toggle-OFF branch and `$firstOpen` never fires (handler-loop-bug-from-generic-helper-default class — the new Stage-4 failure family identified by designer's runtime probe); multi-line CSS rule contract-test regexes must not restrict character classes the rule legitimately contains (AI-769 dark-skin selector-list regex false-fail); complex contract-test regexes with escape gymnastics should split into multiple `assertStringContainsString` calls (AI-770 library iframe regex). Strongest skill-file promotion candidates after this session: contract-test selector-self-match guard (now **11+ recurrences** — AI-518/522/531/691a/697/710/711/712/714/715/716/717/718/719; promoted to a SUMMARY.md Gotchas top-level bullet in the prior maintenance pass) and two-stage CSS shipping (source-level + runtime-probe, established post-task-bc28fd CHANGE). Both meet the 3+ recurrence threshold but the human-decision policy is to formalise via SOUL rows when shifting protocol — leaving as LESSONS+SUMMARY entries for now. **Pattern observation:** Option 2 (SOUL.md + LESSONS.md as canonical keep/discard signal, per-row protocol retired for dispatched work) is now the de-facto operating mode for the **eighth** consecutive review cycle. The 2026-05-17 session shipped the entire 5-ticket image picker arc + one verify-before-accept CHANGE absorption (AI-770 v1 → v2) + 3 documentation maintenance passes (skill-file sync, SUMMARY.md learnings sync, LESSONS.md +3 entries) without any journal pre-task hypothesis row. The drift is now structural, not transitional. Continuing flag-not-formalise.

## Auto-learn 2026-05-17 (review #9)

Reviewed: zero new journal rows since `## Auto-learn 2026-05-17 (review #8)` — the journal still terminates at row 14. Ninth consecutive review-cycle with no new `pending → keep/discard` rows from session work. **Tally:** 0 new discard-recurrence anti-patterns; 0 approach-recurrence best-practices; 0 simplification wins (ΔC = `-`); 0 contradicted-hypothesis knowledge gaps derivable from journal rows alone. Per the anti-fabrication rule from review #2, no retroactive rows are written. **LESSONS.md updates this cycle (0 from journal rows; 6 from session work in the immediately preceding maintenance pass):** lessons surfaced from the 2026-05-17 R10-1 through R10-6 batch arc (AI-779 Slice A / AI-784 AdminFixtureGuard umbrella / AI-785+AI-786 R10-2 Modules+Marketplace / AI-786 CHANGE / AI-788 + AI-788 CHANGE / AI-789 admin-empty-state partial / AI-790 Add-Content x-data leak / AI-792 + AI-793 R10-5/R10-6) — Stage-2 cascade-loss (a CSS colour rule can pass every light-mode test and still render white-on-white in dark mode; AI-786 CHANGE split sizing-at-single-class + colour-at-compound-`!important`); Stage-1 data-shipped-consumer-not-wired (source ships clean + tests green + user sees nothing because `// ->descriptions(...)` is commented at the consumer site; AI-788 CHANGE); Stage-5 template-attribute-escape-leak extends to `//` line comments (AI-790 — robust fix is to extract any long Alpine `x-data` to `Alpine.data('factoryName', () => ({...}))` registration); contract tests asserting ABSENCE of legacy text MUST pre-strip Blade `{{-- … --}}` + JS `/* */` `//` + PHP block comments first (selector-self-match preventive; now 13+ session-occurrences); wizard `$isPost` detection must read BOTH `$record->content_type` AND `$get('content_type')` for the Create vs Edit dual-context (AI-779 Slice A); PHP docblock prose containing literal `*/` terminates the docblock at parse time (AI-790 contract test docblock crash — same family as Alpine x-data `"` corruption + VitePress `{{` rule). Strongest skill-file promotion candidates after this session: the 5-stage failure-family taxonomy itself (catalogued from cumulative 2026-05-16/17 evidence — Stage-1 has 3+ recurrences AI-735/771-CHANGE/788-CHANGE; Stage-2 has 2+ AI-697-v3/AI-786-CHANGE; Stage-5 has 2+ AI-692/AI-790; total 7+ recurrences across families) and the selector-self-match guard (13+ recurrences this session; promoted to a SUMMARY.md Gotchas top-level bullet + AGENTS.md/CLAUDE.md/copilot-instructions.md Code Style bullet in the prior maintenance pass). Both meet the 3+ recurrence threshold but per the established human-decision policy on protocol shifts, leaving as LESSONS + SUMMARY + skill-file (CLAUDE/AGENTS/copilot) entries until explicit human direction to write a `.claude/skills/<slug>/SKILL.md` file. **Pattern observation:** Option 2 (SOUL.md + LESSONS.md as canonical keep/discard signal, per-row protocol retired for dispatched work) is now the de-facto operating mode for the **ninth** consecutive review cycle. The 2026-05-17 R10 batch arc shipped 11+ tickets + 3 CHANGEs absorbed + 4 systemic refactor umbrellas (AdminDisplayName / AdminFixtureGuard / admin-empty-state / ContextAwareFieldConfig flagged for future) + 5-stage failure-family taxonomy formalised + 3 maintenance passes (skill-file sync, SUMMARY.md learnings sync, LESSONS.md +6 entries) — zero journal pre-task hypothesis rows. The drift is structurally stable. Continuing flag-not-formalise.

## Auto-learn 2026-05-17 (review #10)

Reviewed: zero new journal rows since `## Auto-learn 2026-05-17 (review #9)` — the journal still terminates at row 14. **Tenth consecutive review-cycle** with no new `pending → keep/discard` rows from session work; Option 2 (SOUL.md + LESSONS.md as canonical keep/discard signal, per-row protocol retired for dispatched work) has now been the de-facto operating mode for ten consecutive cycles — structurally stable, not transitional. Per the anti-fabrication rule from review #2, no retroactive rows are written. **Tally from journal data alone:** 0 discard-recurrence anti-patterns; 0 approach-recurrence best-practices; 0 simplification wins (ΔC = `-`); 0 contradicted-hypothesis knowledge gaps. **LESSONS.md updates this cycle (0 from journal rows; 6 from session work in the immediately preceding maintenance pass):** lessons surfaced from the 2026-05-17 AI-794..AI-805 chrome arc + Logo trilogy ship sequence — (1) Stage-3 sub-case "viewport-scope leak" formalised (AI-803 P1 HIGHEST — a CSS rule meant for one viewport, declared at top-level, applies everywhere; designer described as "most expensive single-CSS-rule defect this session, silent, source-only-test-passes, only catches at rendered-browser eye"); (2) Blade `@<directive>` tokens inside CSS docblock comments crash the Blade compiler — 2-recurrence promotion (AI-796 + AI-803) with prevention rule extending to all parser-meaningful sequences (Alpine `"`, PHP `*/`, VitePress `{{`); (3) Microweber `_e($str, true)` vs Laravel `__()` for trailing-period translatable strings (AI-796 — Laravel treats trailing `.` as namespace separator + returns empty); (4) PHPUnit `#[DataProvider]` methods run pre-Laravel-boot — 2-recurrence (AI-799 + AI-801, return relative paths + resolve per-test); (5) "Pin the SHAPE not the keyword" contract-test absence-asserts (AI-805 — extends selector-self-match guard family with 4-case examples); (6) `mw()->ui->brand_name()` is canonical — bare `brand_name()` does not exist (AI-794 ship-time crash). **Strongest skill-file promotion candidates:** selector-self-match guard now stands at **16+ session-recurrences** after AI-803/804/805 added 3 more occurrences (Logo trilogy contract tests all hit it preemptively); 5-stage failure-family taxonomy with the new Stage-3 viewport-scope-leak sub-case formalised; Blade `@<directive>`-in-CSS-docblock-comments at 2-recurrence threshold met. Per established human-decision policy on protocol shifts, all three stay as LESSONS + SUMMARY + skill-file (CLAUDE/AGENTS/copilot) entries until explicit human direction to write a `.claude/skills/<slug>/SKILL.md` file. **Session productivity (drift-irrelevant):** the AI-794..AI-805 cycle shipped 11 tickets (AI-794 auth chrome / AI-795 frontend 404 / AI-796 cart empty state / AI-798/799/800 MainDrawer hierarchy + safeRoute + label rename / AI-801 content-type fallback inference / AI-802 spacer label hide / AI-803/804/805 Logo trilogy [P1 + Medium + Low same template]) + 3 systematic maintenance passes (skill-file sync across CLAUDE.md/AGENTS.md/copilot-instructions.md, SUMMARY.md learnings sync, LESSONS.md +6 entries) + this journal review. Combined feature-suite regression peaked at ~192/706 green during the chrome arc. Zero journal pre-task hypothesis rows — confirms the operating mode is structural, not transitional. Continuing flag-not-formalise; the protocol choice has now been emergently retired for 10 review cycles without explicit human formalisation.
