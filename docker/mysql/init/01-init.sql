-- Initial database setup
-- This script runs on first container startup

-- Create additional databases if needed
-- CREATE DATABASE IF NOT EXISTS microweber_test CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
-- GRANT ALL PRIVILEGES ON microweber_test.* TO 'microweber'@'%';

FLUSH PRIVILEGES;
