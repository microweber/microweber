meta {
  name: 02 ping (liveness)
  type: http
  seq: 2
}

post {
  url: {{base_url}}{{mcp_path}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{bearer_token}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "jsonrpc": "2.0",
    "id": 2,
    "method": "ping"
  }
}
