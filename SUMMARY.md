# Project Summary

> Last reviewed: 2026-05-17 — current through AI-794..AI-805 + Logo trilogy + AI-803 CHANGE + AI-805a Path B + AI-816 Save CTA semantic-color contract + AI-817 sr-only label-restoration + AI-818 currency-aware price prefix + AI-819 Filament v5 OKLCH-vs-Tailwind-v3 token format-mismatch + AI-800a sentence-case sweep. Read this file in full at every session start, before TODO.md.

## Project Identity
- Microweber CMS — modular PHP CMS on Laravel 11. Active development branch: `filament-5`. Main remote branch: `master`. Local path: `/home/headless/Documents/GitHub/microweber`.
- Admin UI: Filament v5 (panels: `admin`, `checkout`, `profile`). Public surface: Laravel + filesystem-driven Microweber templates under `userfiles/templates/<template>/`.
- Live Edit: Vue 3 SFC toolbar over a Filament admin frame + Blade iframe canvas, communicating via `window.dispatchEvent(new CustomEvent('<verb>'))`.
- Documentation: VitePress under `docs/` with 9 tier-1 narrative module-doc sets (6 pages each).
- Modal layer: Livewire v4 (`mw-modal.blade.php` generic wrapper) + bespoke `mw-*` modals.

## Architecture
- Microweber is a Laravel 11 modular monolith bootstrapped through `bootstrap/app.php`, which uses `MicroweberPackages\App\LaravelApplication` instead of the stock `Application`.
- Core framework code lives under `src/MicroweberPackages/`, while product features live under `Modules/` and frontend/theme assets live under `packages/`.
- The largest domain models currently include `Modules/Content/Models/Content.php` and `src/MicroweberPackages/User/Models/User.php`.
- Two active Filament v5 panels: `admin` (default) and `checkout`. Scope CSS overrides via `body.fi-panel-admin` / `body.fi-panel-checkout`.
- Filament panel mount paths (confirmed via `->path(...)` in each `*PanelProvider`): admin at `/admin` (default; configurable via `mw_admin_prefix_url()` in `src/MicroweberPackages/Admin/Filament/FilamentAdminPanelProvider.php`), checkout at `/checkout` (`Modules/Checkout/Providers/FilamentCheckoutPanelProvider.php`), profile at `/profile` (`Modules/Profile/Providers/FilamentProfilePanelProvider.php`). Module sub-panels exist for billing (`{adminPrefix}/billing`) and newsletter (`{adminPrefix}/newsletter`). The repo also ships a legacy stub at `app/Providers/Filament/AdminPanelProvider.php:29` mounted at `admin` — superseded by the `src/MicroweberPackages` provider.
- Live Edit is a two-surface system: the Filament admin frame (`AdminLiveEditPage.php`) wraps an iframe canvas (`iframe-page.blade.php`); they communicate via `window.dispatchEvent(new CustomEvent('<verb>'))` (e.g. `liveEditSaveCallMountedAction`, `liveEditUndoLastPublish`, `liveEditInsertLayoutRequest`).
- Vue 3 SFC toolbar lives in `packages/frontend-assets/resources/assets/ui/components/Toolbar/` and is what users see at the top of the Live Edit canvas.
- `Product` extends `Content`; `Cart` is a state manager over Content/Product rows — not a separate data owner. Page/Post bodies live in both `content_body` and `content` columns.
- Color tokens are centralized in `MicroweberPackages\Filament\Themes\MwColors` (primary blue = Bootstrap `#0d6efd` across admin + checkout + public). Use `MwColors::Blue`, never `Color::Blue`.
- mw-modal (Livewire generic modal at `src/MicroweberPackages/Livewire/resources/views/mw-modal.blade.php`) has a focus-trap + Escape + focus-return contract (AI-240); additions like a close-X must filter out of *initial* focus while staying in the Tab cycle.
- Documentation site is VitePress under `docs/`; per-module pages live at `docs/modules/<name>/{index,installation,usage,api,examples,troubleshooting}.md`.
- 95 feature modules under `Modules/` (Accordion, Address, Ai, AiWizard, Attributes, Audio, Background, Backup, BeforeAfter, Billing, Blog, Breadcrumb, Btn, Captcha, Cart, Category, Checkout, Cloudflare, Comments, …) — confirmed total per 2026-05-14 tester audit. Six asset packages under `packages/`: `frontend-assets`, `frontend-assets-libs`, `filament-modules`, `microweber-filament-theme`, `laravel-config-extended`, `laravel-helper-functions`.
- HTTP routes are wired in `bootstrap/app.php` via `withRouting()` and load: `routes/web.php` (public + admin), `routes/api.php` (REST), `routes/ecommerce-api.php` (shop endpoints, applied via the `api` middleware group), `routes/module-api.php` (per-module endpoints), `routes/console.php` (artisan commands).
- DB schema lives in `database/migrations/` (core) plus per-module `Modules/<Name>/database/migrations/` directories where present. SQLite files `database/database.sqlite` and `database/testing.sqlite` exist but the PHPUnit suite targets MySQL `microweber_testing`.
- Admin settings pages extend `MicroweberPackages\Admin\Filament\Pages\AdminSettingsPage` (abstract base); save is Livewire-reactive — subclasses do not implement a submit action.
- `MicroweberPackages\Filament\Support\FilamentRegistry` is the plugin-driven entry point: `registerPage()`, `registerResource()`, `registerWidget()`, `registerPlugin()` register elements onto the admin panel.
- LiveEdit canvas runs 3 concurrent Filament forms; the save-flow specificity ranking (highest priority wins) is `callMountedTableBulkAction` > `TableAction` > `FormComponentAction` > `Action` (mountedAction).
- Sitemap freshness gate (`Modules/Sitemap/Http/Controllers/SitemapHelpersTrait.php::needToUpdateSitemap()`) rebuilds only when the file is missing, unreadable, or older than 3 hours — pinned by `tests/Feature/SitemapCacheTtlContractTest.php`.
- Tier-1 narrative documentation now covers 9 modules at `docs/modules/<name>/` (product, cart, checkout, order, search, seo, sitemap, admin, liveedit). Each module ships 6 pages: index, installation, usage, api, examples, troubleshooting. Landing page at `docs/modules/README.md` groups them by sub-cluster (e-commerce, content-discovery, admin shell); auto-generated 2026-04-25 filesystem index preserved below.
- Multilanguage gate: `MicroweberPackages\Multilanguage\MultilanguageHelpers` controls every locale-aware admin/public surface. Per-locale settings persist as `ModuleOption` rows keyed by locale.
- Skin/template system: public pages render via `userfiles/templates/<template>/` (active template chosen at runtime). The Filament admin panel and the public template renderer are independent rendering pipelines.
- LiveEdit `iframe-page.blade.php` (~2054 lines) is the canonical home of two-surface coordination logic — verb listeners, save bridges, undo/redo snapshots, picker dual-routing. Treat as a single locus; do not split without an architectural dispatch.
- Helper-layer security boundary (ADR-0001): every helper that emits HTML/CSS/JS must apply two-pass escape, URL protocol allowlists (for `href`/`src`/CSS `url()`), and fail-closed sanitization. Context-aware helpers (e.g. `safe_css_url` vs `safe_html_attr`) — one helper per output context.
- A third Filament panel exists: customer Profile (`Modules/Profile/Providers/FilamentProfilePanelProvider.php`, panel id `profile`, mounted at `/profile`). Scope CSS via `body.fi-panel-profile`. Used by AI-518 customer-mobile fixes.
- Touch-target CSS is split across **two build pipelines** based on what owns the rendered HTML — a single mobile-audit fix may need rules in BOTH: public-template HTML → `Templates/Bootstrap/resources/assets/css/public-touch.css` (Vite, served mirror at `public/templates/bootstrap/css/public-touch.css`); Filament-panel HTML → `packages/microweber-filament-theme/resources/assets/css/microweber/mobile-touch.css` (Webpack, served bundle at `public/vendor/microweber-packages/microweber-filament-theme/build/microweber-filament-theme.css`). Reference: AI-518 ships rules in both files.
- Live Edit toolbar consolidation: `ToolbarToolsDropdown.vue` (3-dots menu) is the canonical single dropdown — user-menu items + dark/light theme switcher merge in via `fetchUserMenu()` (calls `api.live-edit.get-top-right-menu`) + `MutationObserver` on `document.documentElement` watching `attributes.class`. The original `#user-menu-wrapper` in `Toolbar.vue` stays in the DOM with `style="display: none;"` for back-compat (task-2026-05-16-3ae87c).
- Live Edit mobile toolbar cascade: `.mw-(admin-)?live-edit-page #toolbar` selector inside `@media (max-width: 768px), (pointer: coarse)` in `live-edit-mobile.css` overrides the bare `#toolbar` declaration from `live-edit-classes.css`. The mobile single-row constraint (`flex-wrap: nowrap !important` + `overflow-x: auto !important` + `flex-shrink: 0` on children) must live in `live-edit-mobile.css` — fixes to the desktop file have no effect at narrow viewports (task-2026-05-16-74c5f5).
- Live Edit right vertical sidebar (`.mw-live-edit-right-sidebar-template-sidebar`): on mobile (`@media (max-width: 768px)`) widens to `72px`; its `.btn-icon.live-edit-toolbar-buttons` children grow to `width: 64px` so 2-line aria-label captions rendered via `::after { content: attr(aria-label); }` wrap without truncating. Mobile readability floor for icon captions is 11px (0.6875rem); 10px is the regression line (task-2026-05-16-8149b5).
- Element Style Editor (ESE) design-token layer: `packages/microweber-filament-theme/resources/assets/css/microweber/element-style-editor.css` opens with a `:root` block of semantic tokens — φ (golden-ratio) spacing scale (`--space-hair` → `--space-2xl`), borders, radii, typography, 12 colour aliases (`--ese-bg` / `--ese-surface` / `--ese-border` / `--ese-accent` …), motion. Dark-theme overrides on `html.dark, html.theme-dark, html[data-theme="dark"]`. `--ese-accent` aliases `#0d6efd` (MwColors::Blue). All new ESE rules consume `var(--ese-*)`, never inline rgba literals (task-2026-05-16-11812b, commit `d8cf5a7bc2`).
- ESE primitive classes (slice 1.3a / task-2026-05-16-f69d54): `.mw-tool-btn` (default 24×24 ghost), `.mw-tool-btn--toggle` (32×32 accent-soft on `.is-active`), `.mw-tool-btn--preset` (32×28 label-sized); `.mw-segmented` + `.mw-segmented__cell` (full-width strip, internal-only dividers). All scoped under `.mw-live-edit-page` per the `live-edit-css-must-be-scoped` skill. Active-state class is `.is-active` (kept alongside legacy `.active` during migration).
- Designer/PM/dev coordination: agent-designer (workspace `/home/headless/Documents/GitHub/designer-agent/`, read-accessible) holds design authority over hierarchy/spacing/colour/interaction/emotional-impact; PM coordinates dispatch/timeline/scope-escalation; dev-a1 implements. Sub-slicing (e.g. ESE 1.3 → 1.3a + 1.3b) is the standard tool when a single dispatch exceeds the slice budget. Joint dispatches (e.g. AI-686 + AI-690 sharing a `:root` rewrite) are accepted only when designer-authorised.
- Agent triumvirate addresses (`.autodev/CONTRACTS.md` is the canonical source): `agent-pm@emailpwd.com` (dispatch + JIRA), `agent-test@emailpwd.com` (audit + verify, dispatches via tester-agent-1), `agent-designer@emailpwd.com` (UX authority since 2026-05-16 / task-f4bf24). I am `dev-a1@emailpwd.com` (alias `agent-a1`). Pure-ack emails get no reply per CONTRACTS.md routing.
- `.autodev/` is the agent-state directory at the repo root. Contains `SOUL.md` (identity + thread history, read first every session), `JOURNAL.md` (hypothesis → keep/discard research log; never delete rows), `CONTRACTS.md` (canonical contact directory), `AGENT_PROFILE.md` + `profile/` (16 protocol files included by `AGENTS.md` via `@`-includes), `messages/` (local-only message artefacts; gitignored). Never write skills here — `.claude/skills/` is the correct location.
- Filament panel boot order: admin (`src/MicroweberPackages/Admin/Filament/FilamentAdminPanelProvider.php`) → checkout (`Modules/Checkout/Providers/FilamentCheckoutPanelProvider.php`) → profile (`Modules/Profile/Providers/FilamentProfilePanelProvider.php`); each provider runs `FilamentRegistry::register{Page,Resource,Widget,Plugin}()` during boot to attach plugin-driven elements. A legacy stub at `app/Providers/Filament/AdminPanelProvider.php` is superseded by the `src/MicroweberPackages` provider.
- Two-group Add-Content modal layout (AI-692 / task-2026-05-16-968a71): each action in `AdminLiveEditPage::addContentAction()` carries `'group' => 'primary'|'secondary'`; the blade partitions into two `<section>` wrappers (primary = `flex flex-col gap-3`, full-width single card; secondary = `grid grid-cols-2 sm:grid-cols-3 gap-3`). Each card carries `data-mw-add-content-group="..."`; Alpine helper `hasVisibleCardsInGroup(group)` filters per group + respects search `q`.
- Live Edit toolbar height is a single `:root` token `--toolbar-height` defined in `packages/frontend-assets/resources/assets/ui/css/index.css` (currently 48px since AI-698a / task-2026-05-16-ec0c87). Cascades to `#toolbar` height, `#live-edit-frame-holder` `top` + `height: calc()`, AI-697 anchored picker `top:`, and mobile media-query rules. Unrelated tokens `--layouts-dialog-toolbar-height` (in-canvas layout picker, 60px) and `--toolbar-static-height` (legacy, 70px) stay separate.
- Live Edit MainDrawer (AI-700 / task-2026-05-16-7326d6) is a `<Teleport to="body">` Vue component (`MainDrawer.vue`) triggered by toolbar `☰` button `#mw-live-edit-main-drawer-button`. 8 nav items wired via `mw.top().app.{domTree,templateSettingsWidget,admin.theme}`. 280px desktop / 100vw mobile; ESC + backdrop close; RTL flip via `html[dir="rtl"]`. ESE stays as its own right-side panel (different surface — focused editor vs navigation).
- Live Edit PageChip (AI-701 / task-2026-05-16-66ceca) — `PageChip.vue` replaces the legacy `<ContentSearchNav>` in the centre toolbar slot; chip with current page title (24-char Unicode-ellipsis) + chevron, popover with 200ms-debounced search input + top-8 results from `get_content_admin?content_type=page` + `+ New page` shortcut. Legacy `<ContentSearchNav>` preserved as hidden back-compat (`#mw-live-edit-search-content` still resolves).
- Live Edit SaveButton state pattern (AI-699 / task-2026-05-16-8b10a3): single `_dirty` reactive flag drives 3 class variants — `.mw-save-button` always, `.mw-save-button--idle` when `_dirty===false`, `.mw-save-button--has-changes` when `_dirty===true` — plus `:aria-pressed`. Visual: solid black pill (`--ese-text` bg, `--radius-pill`); idle = opacity 0.4; has-changes = `animation: mw-save-button-pulse 60s linear infinite` flashing an accent ring in the first 2% of each cycle.
- Admin brand-mark anchor (AI-702 / task-2026-05-16-bcb327): Filament render hook at `PanelsRenderHook::TOPBAR_START` registered in `FilamentAdminPanelProvider.php` BEFORE the existing `admin-top-navigation-actions` Livewire hook (Filament accumulates hooks at the same name in registration order — first-registered renders leftmost). Logo via same fallback chain as `->brandLogo()` (`admin_logo()` → `admin_logo_login()`); brand name via `brand_name() ?: 'Microweber'`; click routes to `url(mw_admin_prefix_url() ?: 'admin')`. CSS `.mw-admin-brand-mark` 24px desktop / 16px mobile; defence-in-depth: every dynamic value passes through `e()`.
- AI-697 anchored Add-Content picker (task-2026-05-16-e3da1a): CSS-only override in `live-edit-module-settings.blade.php` admin-side `<style>` — modal stays a Filament `Action` mounted via `->extraModalWindowAttributes(['class' => 'mw-content-form-modal mw-content-picker-modal'])`. CSS scoped via `:has(> .fi-modal-window-ctn .mw-content-picker-modal)` on outer `.fi-modal` so other modals keep centered + dimmed defaults. Anchors top-left near +ADD button. Mobile ≤ 768px keeps centered modal until AI-695 bottom-sheet ships.
- Image picker (`mw.filePicker`) arc closed 2026-05-17 (5/5 dispatch-named tickets): AI-768 mobile tab nav `:not()` scope guard (`bceba95252`) → AI-769 dialog dark-skin (`4895cbef28`) → AI-770 v2 toggle:false + direct `$firstOpen` (`5f18c19220`) → AI-771 single-column AI tab per designer Option A (`0d047f4801`) → AI-772 URL tab label rename + helper text + https placeholder (`fda2a5a8e9`). One CHANGE absorbed mid-arc (AI-770 v1→v2). One follow-up filed (AI-769a `.form-control-live-edit-input` dark-skin selector gap, dispatch-pending).
- `mw.tabs()` helper at `packages/frontend-assets/resources/assets/tools/tabs.js` (lines ~87-107): default `toggle: true` causes click on the already-active tab to enter the toggle-OFF branch (removes `.active`, hides content) and the `$firstOpen` lazy-mount event does NOT fire in that branch. Pass `toggle: false` on init for deterministic first-open on the initially-active tab. Combined with `$(scope).trigger('$firstOpen', [section, type])` direct dispatch + `__navigation_first.push(idx)` double-fire guard, this is the canonical lazy-iframe-mount pattern (AI-770 v2 / task-2026-05-17-49ad9d).
- `mw.dialog` dark-mode skin lives in `packages/microweber-filament-theme/resources/assets/css/microweber/general-styles.css` (after the AI-740 backdrop override) and applies to `.mw-dialog-skin-default` shared by filepicker / prompts / confirms. Covered selectors: `.mw-dialog-holder`, `.mw-modal-title-holder`, `.mw-dialog-header`, `.mw-filepicker-component-navigation-header a`, `input.form-control`, `textarea.form-control`, `.mw-dialog-bottom-buttons`, `.mw-dialog-footer`, `.mw-filepicker-component-thumbnails`. The narrower `.form-control-live-edit-input` class is NOT covered — AI-769a follow-up filed (AI-769 / task-2026-05-17-df485c).
- Image picker AI tab structure (AI-771 / task-2026-05-17-23e0ee): outer wrapper `<div class="mw-image-picker-ai w-full">` (Tailwind), inner CSS `.mw-filepicker-ai-tab-wrap { max-width: none; margin: 0 }` (was `max-width: 400px; margin: auto`). Secondary fields KEEP `.mw-filepicker-ai-field-narrow { width: 200px }` so Width / Height / Reference image / aspect ratio stay narrow for visual hierarchy against the full-width Generate button. Two-constraint stack — both outer Tailwind AND inner CSS were pinning ≈400px; both had to be un-wired.
- 2026-05-17 session catalogued **5 distinct failure-family stages** that recur across the codebase — name them in dispatches/PRs so reviewers know what to look for. **Stage-1 data-shipped-consumer-not-wired**: source ships + tests pass + consumer call commented or missing (AI-735→AI-793 admin route propagation; AI-771 CHANGE cross-package `@import`; AI-788 CHANGE `->descriptions(CustomFieldTypes::class)` commented at `ListCustomFields.php:266`). **Stage-2 cascade-loss dark-mode-only**: light-mode passes + dark-mode washes out (AI-697 v3, AI-786 CHANGE — split single rule into sizing-at-single-class + colour-at-compound-`!important`). **Stage-3 wrong-surface-mount** including the propagation-without-renderer-update sub-case (route propagates correctly but generic renderer ignores the new branch). **Stage-4 handler-loop-from-generic-helper-default**: generic helper option default routes interaction into a silent no-op branch (AI-770 v2 `mw.tabs({ toggle: true })` default). **Stage-5 template-attribute-escape-leak**: HTML attribute parser corrupts long Alpine `x-data` containing embedded `"` (AI-692 `/* */` block-comment; AI-790 `//` line-comment with quoted prose — fix by extracting to `Alpine.data('factory-name', () => ({...}))` registration).
- Shared admin helpers added 2026-05-17: `MicroweberPackages\Filament\Support\AdminDisplayName` (canonical CamelCase/snake_case/kebab-case → human-readable normaliser; 25-acronym whitelist keeps AI/SEO/FAQ/URL/API/HTML/RTL/… uppercase; null/whitespace returns empty string; idempotent on Title Case input; call via `TextColumn::make('name')->formatStateUsing(fn (?string $state): string => AdminDisplayName::format($state))`) and `MicroweberPackages\Filament\Support\AdminFixtureGuard` (Faker-leak filter; 158-word vocab; `shouldRenderItem($item)` / `looksLikeFakerLorem($text)` / `filterByTitle($collection, $titleField)` helpers used on admin lists pulling from Faker-seeded tables). Reusable empty-state Blade partial at `src/MicroweberPackages/Filament/resources/views/filament-forms/partials/admin-empty-state.blade.php` (carries `__heading` / `__body` / optional `__cta` slot) — consumed by both admin-shell error pages (AI-793 admin 404 view) and Filament resource empty states. References: AI-784/785/789.
- `Modules\Content\Models\Content::createDefaultBlogPage()` MUST set `$blogPage->layout_file = 'blog.blade.php'` before save so the renderer routes to the blog template instead of falling through to the home hero. Existing installs with `layout_file = null` Blog pages need the AI-792b backfill migration. Reference: AI-792 / task-2026-05-17-4e9d1b.
- Admin 404 propagation gate: `src/MicroweberPackages/Frontend/routes/web.php` Route::fallback() carries an admin-prefix branch — `if (request()->is(mw_admin_prefix_url() . '/*')) { return response()->view('admin.errors.404', […], 404); }` — BEFORE the generic FrontendController fallthrough. Renderer view at `resources/views/admin/errors/404.blade.php` renders an admin-styled 404 with `.mw-admin-empty-state__heading` + "Back to dashboard" CTA + `X-Fallback-Message: admin-404` header + `noindex` meta. Reference: AI-793 / task-2026-05-17-a596d2 (Stage-3 propagation-without-renderer-update fix).
- `Modules\Content\Filament\Admin\ContentResource::parentPageSection()` detects `$isPost` from BOTH `$record->content_type` AND `$get('content_type')` so the wizard renders correctly on initial form mount (no `$record`) AND on edit (has `$record`). When isPost: returns TWO sub-Sections (Parent page `skipCategories=true singleSelect=true contentType='page'` + Categories `skipCategories=false singleSelect=false`). Non-post path preserved verbatim. Reference: AI-779 Slice A / task-2026-05-17-4c289e.
- Stage-3 sub-case "**viewport-scope leak**" formalised 2026-05-17 (AI-803 / task-fa5dc3): a CSS rule designed for a specific viewport but declared OUTSIDE its intended `@media` block applies to ALL viewports — source-only tests pass; only catches at cross-viewport rendered-browser probe. Sibling to the existing Stage-3 propagation-without-renderer-update sub-case. Pre-fix `.logo-module { min-width: 0; overflow: hidden }` was declared unconditionally despite a docblock saying "for narrow viewports" — collapsed parent flex column to 0×0 at every viewport and made the SVG logo invisible. Designer description: "most expensive single-CSS-rule defect this session — silent, source-only-test-passes, only catches at rendered-browser eye." Fix shape: gate narrow-viewport rules inside `@media (max-width: 575px)` (Bootstrap 5 `sm` breakpoint).
- **Logo template trilogy** (`Modules/Logo/resources/views/templates/default.blade.php` — all 3 shipped as separate commits on the same template 2026-05-17): (1) AI-803 [P1 HIGHEST] / task-fa5dc3 — gate `.logo-module { min-width: 0; overflow: hidden }` + `.logo-link` ellipsis chain inside `@media (max-width: 575px)`; (2) AI-804 Medium / task-488fa3 — three-tier alt fallback `alt="{{ isset($text) && $text !== '' ? $text : (get_option('website_title', 'website') ?: 'Home') }}"` for WCAG 2.1 SC 1.1.1; (3) AI-805 Low / task-e2e29a — collapsed dead `@if(is_live_edit()) href="{{ site_url() }}" @else href="{{ site_url() }}" @endif` to single `<a href="{{ site_url() }}" class="logo-link">`. **Path B (live-edit-disabled navigation via `is_live_edit() ? '#' : site_url()`) is filed as AI-805a follow-up** in the template Blade comment — needs product call before shipping (behaviour change).
- Frontend 404 short-circuit gate (AI-795 / task-c54f1f): `src/MicroweberPackages/App/Http/Controllers/FrontendController.php` carries a short-circuit branch inside the `if (!is_admin())` arm, AFTER template-404.php checks: when no content match is found, render `view('frontend.errors.404', ['extendsView' => ..., 'requestedUrl' => ...])` with 404 status + `X-Robots-Tag: noindex, nofollow` + `X-Fallback-Message: frontend-404` headers. Renderer view at `resources/views/frontend/errors/404.blade.php` extends the active template master with Bootstrap fallback. Companion to the AI-793 admin-prefix 404 propagation gate.
- Active-template-master extends with Bootstrap fallback (AI-794 / task-f141ff + AI-795 / task-c54f1f): Blade views consumed by both frontend and auth surfaces extend `$extendsView ?? 'templates.bootstrap::layouts.master'` — the active template's master if available, falling back to Bootstrap when the active template doesn't define one. Pattern lineage: AI-757. Reference views: `src/MicroweberPackages/User/resources/views/layout.blade.php` (auth chrome), `resources/views/frontend/errors/404.blade.php` (frontend 404).
- Live Edit MainDrawer 3-section hierarchy (AI-798/799/800 / task-7a9913 / 918e58 / f39d53): drawer renders 3 labeled `<h3 class="mw-main-drawer__section-header">` sections (Edit / Navigate / Preferences) with per-section affordances. 9 `data-mw-drawer-item="<slug>"` markers anchor contract tests. **Cross-component CustomEvent verb-bridge** extended — new "Pages" item dispatches `mwOpenPageChip` CustomEvent on `window` to trigger Toolbar `PageChip` (extends the existing `liveEditSaveCallMountedAction` / `liveEditUndoLastPublish` / `liveEditInsertLayoutRequest` family). `safeRoute(name, fallback)` closure wraps `window.route()` (Ziggy) in try/catch with literal-path fallback — survives missing Ziggy bundle or unknown route names. `readMenuUrls()` uses `safe = item.href && item.href !== '#' ? item.href : null` gate so live-edit `#` placeholders never become nav targets. Label renames per spec: "Back to admin" / "Templates & layouts" / "Theme settings" / "View public site".
- Content-type fallback inference in canvas templates (AI-801 / task-fe8f9e): `Modules/Content/resources/views/templates/{default,skin-1,masonry,dictionary,search,sidebar}.blade.php` infer `$mwAi780Type` from `$params['type']` when `$params['content_type']` is missing — `match ($params['type'] ?? null) { 'posts' => 'post', 'pages' => 'page', 'products' => 'product', default => null }`. Parser invariant pinned at `src/MicroweberPackages/App/Utils/ParserLoadModuleTrait.php:405-407` which populates `$attrs['type'] = $module_name` from `<module type="...">`. Pre-fix, the posts module rendered the default empty-state branch instead of the post-specific one (Stage-1 fallback inference defect).
- Spacer label hide-by-default pattern (AI-802 / task-e06dec): `packages/frontend-assets/resources/assets/css/scss/liveedit.scss` — `.mw-le-spacer-info-content { opacity: 0; transition: opacity .2s ease }` + 3 show triggers (`.mw-le-spacer:hover` / `.mw-le-spacer.mw-le-spacer-resizing` / `body.mw-editmode-show-spacer-labels`) + `@media (prefers-reduced-motion: reduce) { transition: none }` guard. Placed AFTER existing rules for source-order cascade tiebreak.
- Cart empty-state chrome (AI-796 / task-0e6cfa): `Modules/Cart/resources/views/page.blade.php` gates checkout CTA via `@if($mwAi796HasItems = !empty(mw()->cart_manager->get() ?? []))`. `Modules/Checkout/resources/views/livewire/cart-items.blade.php` else-branch renders `.mw-cart-empty` chrome — heading "Your cart is empty" + body via Microweber `_e('...', true)` (Laravel `__()` returns empty on trailing-period keys) + single "Continue shopping" CTA. Brand-blue `#0d6efd !important` overrides on `.mw-cart-standalone-checkout-cta` + `.mw-cart-empty-cta`.
- **Save CTA color contract — primary-blue NOT success-green** (AI-816 / task-2b1020, lineage AI-209/737/704/731/794): unsubmitted-form CTAs (Save / Save & Continue / Create) MUST `->color('primary')` (brand-blue `#0d6efd` / `Color::Primary` / `MwColors::Blue`). `->color('success')` (green) reserved for post-submit semantic-success states ONLY — saved, published, approved, mark-paid, marketplace install/enable, widget chart tokens. Designer framing: green on UNSUBMITTED CTA conflates "click to save" with "saved". **Class-inheritance carries the fix**: `CreateProduct extends CreateContent` + `CreatePage extends CreateContent`, so flipping `CreateContent::getHeaderActions()` Save action fixes Post + Page + Product in one edit. Slice B audit-the-base-class approach surfaced 5 in-scope siblings (CreateContent saveContent / EditContent EditAction / AdminLiveEditPage in-canvas Create modal / ContentTableList CreateAction + EditAction) all sharing the same obsolete "match the green main-toolbar SAVE pill" rationale — obsoleted by AI-699 (toolbar SAVE became BLACK pill 2026-05-16). Pinned by `Admin2b1020AI816SaveCtaPrimaryColorContractTest` + regression guards for stays-green surfaces (Place Order CheckoutWizard:451 per AI-209, Publish bulk ContentResource:2107).
- **Standing chrome-application checklist for URL fall-through paths** (Stage-3 propagation-without-renderer-update, 3-recurrence promotion AI-735 → AI-793 → AI-795): every URL fall-through path (admin 404 / frontend 404 / auth-expired / cart-empty / etc.) needs its OWN chrome-application, never assumed-by-inheritance. Each path MUST emit (1) extends active-template-master with Bootstrap fallback (AI-757 pattern); (2) semantic chrome container (`.mw-admin-empty-state` / `.mw-auth-card` / `.mw-frontend-404` / `.mw-cart-empty`); (3) recovery CTA; (4) correct HTTP response code + headers (`X-Robots-Tag: noindex, nofollow` + `X-Fallback-Message: <surface>-404` for 4xx); (5) contract test pinning all four. Skip ANY one and the path lands as blank/raw response — the original AI-735/793/795 defects.
- **AI-805a Path B closes Logo trilogy** (task-af2b73, commit `fb0a119a02`): `Modules/Logo/resources/views/templates/default.blade.php` anchor renders `<a href="{{ is_live_edit() ? 'javascript:void(0)' : site_url() }}" class="logo-link"@if(is_live_edit()) title="Use the menu to navigate" aria-disabled="true"@endif>`. `javascript:void(0)` (not `'#'`) chosen — `'#'` jumps to top of page if unhandled. Designer's own audit hit the data-loss bug twice in one day. Public-mode rendering unchanged. Three pin-evolutions in `LogoE2e29aAI805DeadBranchCleanupContractTest` (single test file, no parallel v2 tests).
- **AI-803 CHANGE runtime parent-flex-collapse fix** (task-5b0a92, commit `852a8549ba`): the original AI-803 ship correctly gated narrow-viewport rules inside `@media (max-width: 575px)`, but desktop probe found img STILL 0×0 because parent template wrapper carries Bootstrap `col-xl-4 w-auto` (`w-auto !important` wins by Bootstrap utility source-order specificity → parent width = content width). Without `display: inline-block` on `.logo-link`, anchor defaults to `display: inline` and collapses. Fix: top-level `.logo-module .logo-link { display: inline-block; }` (mirrors the same declaration already in the ≤575px block). **Stage-2 cascade-loss family promoted to 3-recurrence** (AI-697 v3 + AI-786 CHANGE + AI-803 CHANGE) — pattern signature "source change correct but a sibling rule (or parent flex context) needed runtime adjustment."
- **AI-817 sr-only label-restoration for WCAG 3.3.2** (task-04f015, commit `c90bb04dad`): `Modules/Content/Filament/Admin/ContentResource.php::compactTitleOnlySection()` + `Modules/Category/Filament/Admin/Resources/CategoryResource.php` Title inputs replaced `->hiddenLabel()` (which renders no label AT users can read — WCAG 3.3.2 Level A failure) with `->label('Title')` + `->extraFieldWrapperAttributes(['class' => 'mw-fb-title-wrap'])` + `'aria-label' => 'Title'`. CSS in `live-edit-module-settings.blade.php` sr-only-hides `.mw-fb-title-wrap label` via `position: absolute; width: 1px; height: 1px; overflow: hidden; clip: rect(0,0,0,0);` — visually hidden but read by AT (NOT `display: none` which removes from accessibility tree). Class inheritance (`CreatePage extends CreateContent` + `CreateProduct extends CreateContent`) carries the fix to Post + Page + Product (Slice B audit-the-base-class).
- **AI-818 currency-aware Filament price prefix** (task-fc0b22, commit `82584acac8`): canonical helper is `currency_symbol()` (Shop module wrapper); shop-default shape `->prefix(fn () => currency_symbol() ?: '$')`; multi-currency wizard shape `->prefix(fn (Forms\Get $get) => static::resolveCurrencyPrefix($get('currency')))` with `currency` Select carrying `->live()`. Reference helper: `Modules/Billing/Filament/Admin/Resources/SubscriptionPlanResource.php::resolveCurrencyPrefix(?string $currency): string` — 6-currency map (USD `$` / EUR `€` / GBP `£` / CAD `C$` / AUD `A$` / JPY `¥`) + shop-default fallback. Surfaces shipped: ContentResource pricingSection + productDetailsSection, InvoiceResource line-item, SubscriptionPlanResource Price + Trial-price. Designer dispatches sometimes cite `option_get('currency_symbol', 'shop')` — `currency_symbol()` is the documented canonical name.
- **AI-819 Filament v5 OKLCH vs Tailwind v3 RGB-triplet token format-mismatch** (task-9a12c2, commit `32097ab25d`): Filament v5's `ColorManager::convertToOklch()` emits `--primary-NNN: 12.34% 0.123 45.67;` (OKLCH-composable) but `tailwind.config.js` consumes `rgba(var(--primary-500), <alpha-value>)` (Tailwind v3 RGB-triplet format). Incompatible — Filament's emit silently falls back to its default blue (`#4299E1`) instead of brand `#0d6efd`. Fix: explicit `:root { --primary-50: 231, 241, 255; ... --primary-950: 1, 10, 25; }` block at top of `packages/microweber-filament-theme/resources/assets/css/microweber-theme-v3.scss` anchored at `MwColors::Blue` values verbatim (slot 500 = `13, 110, 253` = `#0d6efd`). Bypasses Filament's runtime emit. **New Stage candidate "framework-emit-format-mismatch"** — framework upgrade silently changes the emit format of a token a consumer pipeline depends on. Diagnostic-surface heuristic: AI-816 painted most Save CTAs primary-blue via `->color('primary')`, so Image-add (lacking AI-816) was the surface where `fi-color-primary` still surfaced the underlying defect.
- **AI-800a sentence-case + plural-plural label sweep cascade** (task-bce4b7, commit `0ff6832c0d`): cross-surface case sweep after AI-708/AI-800 heading rename. `RightSidebar.vue:20` "Theme Settings" → "Theme settings"; `bootstrap.js:95` controlBox title "Template & Layout" → "Templates & layouts" (sentence case + plural-plural matching MainDrawer per AI-708/AI-800). **Sidebar/menu IA-refactor pattern now 3-recurrence** (AI-798 / AI-826 / AI-836) — when a rename ships at one surface, audit every other surface emitting the same string.

## Module Map (selected — full list under `Modules/`)
- **Content & taxonomy:** Content, ContentData, ContentDataVariant, ContentField, Category, Comments, ContactForm, CustomFields, Tags.
- **Pages / posts / products inheritance:** `Product extends Content`; `Page` is a content_type variant of Content; `CreateProduct extends CreateContent`, `CreatePage extends CreateContent` (Slice B audit-the-base-class applies to the Filament resource pages).
- **E-commerce:** Cart, Checkout, Coupons, Currency, Billing, Address, Country, Order, Payment, Shipping, Invoice, ProductPricingRule.
- **Public-page surfaces:** Logo, Menu, Pictures (gallery), Slider, Search, Seo, Sitemap, Breadcrumb, FAQ, Newsletter, Rating, SocialLinks, Tabs, Teamcard, Testimonials, Video, GoogleMaps.
- **Media & UI primitives:** Media, Audio, Background, BeforeAfter, Btn, Accordion, Captcha, CookieNotice.
- **Admin / shell:** Admin, AdminSettings, Marketplace, ModuleConfiguration, Permission, Role, Workflow, Translation, ErrorTracking, SiteStats, Multilanguage, Profile.
- **Operations / infra:** Backup, BackupSchedule, Cloudflare, Ai, AiWizard.
- **Live Edit:** lives at `src/MicroweberPackages/LiveEdit/` (not under `Modules/`) — two-surface system (Filament admin frame + Blade iframe canvas) bridged by `window.dispatchEvent(new CustomEvent('<verb>'))`.
- **Pattern:** each module owns its Filament resource under `Modules/<Name>/Filament/Admin/<Resource>/Pages/`, its model under `Modules/<Name>/Models/`, and optionally its own migrations directory `Modules/<Name>/database/migrations/`. Public-surface modules also carry `Modules/<Name>/resources/views/templates/*.blade.php` (`default.blade.php` plus per-skin variants) consumed by the parser via `<module type="<name>">`.

## Naming & Conventions
- PHP tests use PHPUnit 11 with `#[Test]` attributes; existing class-style tests prefer descriptive `it_*` method names.
- The repository’s broad PHP suites are defined in `phpunit.xml`, and the memory-safe full-suite runner is `./run-tests.sh`.
- Conventional Commits are already documented in `CONTRIBUTING.md` and used for task-by-task commits.
- Filament action keys are camelCase ending in `Action` (e.g. `addImageAction`, `generateAction`, `saveContentAndGoLiveEditIframe`).
- Filament page classes mirror Filament defaults: `Create<Name>`, `Edit<Name>`, `<Name>Resource`; pages register form via `static::getResource()::form($schema)`.
- CSS class prefix `mw-*` for project-bespoke styles (never inside the Filament `.fi-*` namespace). HTML data attributes use `data-mw-*`. localStorage keys use `mw-*`.
- Toolbar Vue back-compat hooks (e.g. `id="toolbar-user-menu-button"`, `class="mw-le-hamburger"`) are referenced by external code — preserve them when restyling toolbar elements.
- Picker synonym maps (e.g. in `add-content-modal.blade.php`) are keyed by action name with a space-joined synonym string as the value — used by the Alpine search to match user intent.
- Resource list empty-state CTA class is `.mw-table-empty-cta` — NOT `.mw-empty-state-cta`, which is owned by `DashboardEmptyStateWidget` (`microweber-theme-v3.scss`) and silently overrides any duplicate. Use distinct prefixes (`mw-table-*`, `mw-dashboard-*`, `mw-le-*`) to scope by surface.
- Per-task source-comment marker: every UX/UI fix embeds `task-YYYY-MM-DD-XXXXXX` in (a) commit subject, (b) contract-test docblock + filename slug, (c) at least one source-side comment near the changed line — enables single-pass audit grep across blade/CSS/Vue/PHP/test surfaces.
- Live Edit toolbar Vue back-compat hooks (post AI-700 / AI-701 chrome refresh): `#user-menu-wrapper`, `#toolbar-user-menu-button`, `#user-menu nav`, `.mw-le-hamburger`, legacy `<ContentSearchNav>` wrapper (`#mw-live-edit-search-content`) are all kept `style="display:none;" aria-hidden="true"` — pinned by `tests/Browser/AdminLiveEditDropdownAndButtonsTest.php` Dusk test and `SettingsCustomize.vue:651`. Visible new toolbar element IDs: `#mw-live-edit-main-drawer-button` (AI-700 ☰), `.mw-page-chip` (AI-701), `.mw-save-button` (AI-699), `.mw-admin-brand-mark` (AI-702 admin shell).
- SaveButton state-class triad: `.mw-save-button` (always) / `.mw-save-button--idle` / `.mw-save-button--has-changes` — keep static legacy classes (`btn btn-dark live-edit-toolbar-buttons`) on the `class="..."` attribute and put state classes on the `:class` binding (one boolean key per class) so contract tests can pin them independently.
- Sub-slice ID convention when a multi-item ticket splits: `[ticket]a` (immediately shippable), `[ticket]b` (paired with dep), `[ticket]c` (deferred). Each slice gets its own commit, contract test, and task-id marker. Document the rationale up-front in the SHIP report + contract-test docblock. Examples shipped: ESE 1.3 → 1.3a + 1.3b (designer-accepted); AI-698 → a (height-lock) + b (item 3 hamburger, paired with AI-700) + c (items 1+2 deferred).
- Contract-test class naming (recurring 2026-05-16 cycle): `<Slug><TaskSuffix><AINumber><Concern>ContractTest` — e.g. `AdminBcb327AI702BrandMarkContractTest`, `LiveEdit7326d6AI700MainDrawerContractTest`, `ESEf69d54Slice13aMwToolButtonContractTest`. Filename slug carries the task-id 6-char suffix so the test is grep-discoverable from the source-side marker.
- Commit-subject format for UX/chrome work: `<type>(<scope>): task-<XXXXXX> <imperative summary>` — e.g. `feat(live-edit): task-7326d6 consolidate Template Settings + admin nav into MainDrawer`. Scope is the surface name (`live-edit`, `admin`, `ese`, `add-content`). The task-id 6-char suffix in the subject lets a future `git log --grep task-XXXXXX` recover the full ship from any surface.
- Microweber helper-idiom canonical names (recurring 2026-05-17): brand name is `mw()->ui->brand_name()` (NEVER bare `brand_name()` — no such global function); admin logo is `mw()->ui->admin_logo()` / `mw()->ui->admin_logo_login()`; site settings via `get_option(key, group)` (NOT `option_get` — back-compat alias exists but `get_option` is the documented canonical); translatable strings with trailing periods via `_e('...', true)` (Microweber translate+echo) because Laravel `__('Browse our products and add items.')` returns empty (trailing `.` treated as namespace separator).
- CustomEvent verb-bridge family for cross-component triggers: `liveEditSaveCallMountedAction` / `liveEditUndoLastPublish` / `liveEditInsertLayoutRequest` / `mwOpenPageChip`. New verbs MUST be wired in BOTH places (dispatcher + listener); contract test asserts `window.dispatchEvent(new CustomEvent('<verb>'))` in source AND `window.addEventListener('<verb>', ...)` in target. Extended 2026-05-17 with AI-798 MainDrawer→PageChip via `mwOpenPageChip`.

## Key Files
- `public/index.php` — HTTP entry point served by Apache/nginx; boots Laravel via `bootstrap/app.php`.
- `artisan` — CLI entry point for migrations, queue workers, scheduler, custom commands.
- `bootstrap/app.php` — Laravel bootstrap; instantiates `MicroweberPackages\App\LaravelApplication`; wires routes via `withRouting()`; configures rate limiters.
- `routes/web.php` — public + admin web routes.
- `routes/api.php` — base REST API routes.
- `routes/ecommerce-api.php` — shop/cart/checkout endpoints (loaded inside the `api` middleware group).
- `routes/module-api.php` — per-module API endpoints.
- `routes/console.php` — artisan command bindings.
- `config/app.php` — service providers, aliases, app-level config (companion config files for each subsystem live alongside).
- `.env` / `.env.example` — environment configuration. Contains DB credentials, app key, mail driver, and (per `.gitignore`) is never committed. Tests read from `phpunit.xml` env block or `.env.testing`.
- `database/migrations/` — core schema migrations. Per-module migrations live under each `Modules/<Name>/database/migrations/`.
- `phpunit.xml` — canonical PHPUnit suite layout for Unit, Feature, Core, module groups, and Templates.
- `run-tests.sh` — split-process runner used to avoid PHP memory fragmentation during large suite runs.
- `package.json` — root frontend/docs/security npm scripts. Root `npm run build` shells out to `run-build.js`; `npm run dev` shells out to `run-dev.js`.
- `run-build.js` / `run-dev.js` — orchestrate per-package builds across `packages/<pkg>/`.
- `.github/workflows/cicd-pipeline.yml` — CI stages for quality, security, and automated tests.
- `src/MicroweberPackages/LiveEdit/Filament/Admin/Pages/AdminLiveEditPage.php` — Live Edit admin frame (actions, modals, save bridge).
- `src/MicroweberPackages/LiveEdit/resources/views/iframe-page.blade.php` — Live Edit canvas iframe (event listeners, in-canvas glue).
- `src/MicroweberPackages/LiveEdit/resources/views/add-content-modal.blade.php` — picker modal with Alpine search + synonym map.
- `src/MicroweberPackages/Livewire/resources/views/mw-modal.blade.php` — generic Livewire modal wrapper (focus trap, backdrop dismiss, close X).
- `src/MicroweberPackages/Filament/Themes/MwColors.php` — canonical color token authority.
- `src/MicroweberPackages/Admin/Filament/Pages/Login.php` — admin login (autocomplete + inputmode tokens).
- `src/MicroweberPackages/Filament/resources/views/filament-forms/components/mw-media-browser.blade.php` — media browser dropzone affordance.
- `Modules/Content/Filament/Admin/ContentResource/Pages/CreateContent.php` — Content create page; uses `getInitialData()` to pre-fill from query string.
- `packages/frontend-assets/resources/assets/ui/components/Toolbar/{Toolbar,SaveButton,AddContentButton}.vue` — Live Edit toolbar Vue SFCs.
- `packages/frontend-assets/resources/assets/css/microweber/css/ui.css` — toolbar styles; mirror new toolbar rules in `packages/microweber-filament-theme/resources/assets/css/microweber/general-styles.css` when admin needs them too.
- `docs/.vitepress/config.js` — VitePress sidebar registry; append a sidebar entry whenever new module docs land.
- `.autodev/SOUL.md` / `.autodev/JOURNAL.md` / `.autodev/CONTRACTS.md` — agent identity (thread history table; read FIRST every session), research log (hypothesis → keep/discard rows; never delete), and contact directory (canonical agent + human addresses; never invent).
- `.autodev/AGENT_PROFILE.md` + `.autodev/profile/00-identity.md` … `16-journal.md` — 17-file protocol index that drives the agent's working contract; all included into `AGENTS.md` via `@`-includes.
- `NOVICE_REPORT.md` — novice-customer persona audit findings (P0..P3).
- `TODO.md` / `DONE.md` — live task queue and append-only archive.
- `CHANGELOG.md` — completed-task technical record (prepended newest-first; written before each commit).
- `LESSONS.md` — correction patterns and guardrails (append-only, newest at top).
- `TROUBLESHOOTING.md` / `SETUP.md` — living failure register and onboarding guide.
- `Modules/Sitemap/Http/Controllers/SitemapHelpersTrait.php` — sitemap cache-TTL gate (3hr file-mtime threshold).
- `tests/Feature/SitemapCacheTtlContractTest.php` — reference pattern for trait-only PHPUnit contract tests (anonymous class consumes the trait, no DB, no Laravel boot).
- `tests/Feature/Ai518CustomerTouchTargetContractTest.php` — reference for two-surface bounded-slice contract test (mobile-touch.css + public-touch.css) with admin/checkout boundary guard. Slice-bounding pattern: scope an assertion block by `strpos` from marker to closing brace, NOT to EOF.
- `tests/Feature/Ai519CouponCoverageContractTest.php` — reference for coverage-analysis regression-guard test (no CSS changes; pins the inheritance chain when recon shows inherited coverage; flags partial-coverage gaps as `AI-NNNa follow-up candidate` in docblock).
- `Modules/Profile/Providers/FilamentProfilePanelProvider.php` — customer Profile Filament panel registration.
- `docs/modules/README.md` — tier-1 narrative-docs landing page with sub-cluster tables.
- `src/MicroweberPackages/Admin/Filament/Pages/AdminSettingsPage.php` — abstract base for admin settings pages.
- `src/MicroweberPackages/Filament/Support/FilamentRegistry.php` — plugin-driven Filament registration entry point.
- `docs/adr/0001-helper-layer-security.md` — helper-layer security model ADR (two-pass escape, URL allowlists, sanitizer fail-closed, context-aware helpers).
- `packages/frontend-assets/resources/assets/css/microweber/css/design-system.css` — canonical CSS token authority (`--color-*`, `--radius-*`, `--space-*`). Override at the token, not per-template.
- `packages/frontend-assets/resources/assets/css/microweber/css/public-touch.css` + `mobile-touch.css` — public-facing UX rules; mobile-only and touch-target adjustments scope here.
- `userfiles/templates/<template>/` — active skin/template renders public pages (filesystem-driven, not in Filament).
- `src/MicroweberPackages/Multilanguage/MultilanguageHelpers.php` — locale gate for admin and public surfaces (`shouldShowOnAdminEditor()` etc.).
- `packages/microweber-filament-theme/resources/assets/css/microweber/live-edit-mobile.css` — mobile-only overrides for Live Edit toolbar (horizontal-scroll, no-wrap) + right vertical sidebar (label-wrap, widened columns). Webpack pipeline; wins the cascade for `.mw-live-edit-page #toolbar` at <=768px / pointer-coarse.
- `packages/microweber-filament-theme/resources/assets/css/microweber/live-edit-classes.css` — Live Edit toolbar + canvas baseline (desktop-default) styles.
- `packages/frontend-assets/resources/assets/ui/components/Toolbar/ToolbarToolsDropdown.vue` — Live Edit 3-dots admin tools menu; also hosts user-menu items + theme switcher post-task-3ae87c.
- `Modules/SiteStats/Filament/Widgets/{Locations,Languages,RecentVisitors}Widget.php` — reference for Filament `TextColumn::formatStateUsing` unknown-value normalization + query-level junk-row filters.
- `packages/microweber-filament-theme/resources/assets/css/microweber/element-style-editor.css` — Element Style Editor styles. First ~100 lines hold the Phase 1 design-token layer (φ spacing scale, semantic colour aliases, motion, dark-theme override); all new ESE rules consume `var(--ese-*)`.
- `packages/frontend-assets/resources/assets/ui/apps/ElementStyleEditor/` — ESE Vue 3 app root (Typography, Spacing, Background, Border, Presets sub-components). Vite pipeline.
- `src/MicroweberPackages/Filament/resources/views/filament/components/layout/live-edit.blade.php` — Live Edit admin frame layout (sidebar slot; canonical home of the Tailwind v4 `translate: none !important` override).
- `src/MicroweberPackages/LiveEdit/resources/views/add-content-modal.blade.php` — Add-Content picker (two-group layout + Alpine `hasVisibleCardsInGroup(group)` + `JSON.stringify(group)` safe-attribute-selector pattern).
- `tests/Feature/AddContent968a71AI692TwoGroupLayoutContractTest.php` — reference for the no-block-comments-in-xdata regression guard + two-group partition assertion shape.
- `tests/Feature/ESE11812bSlice11TokensContractTest.php` — reference for ESE design-token contract (pins token names + values + dark-theme overrides + `prefers-reduced-motion` collapse + MwColors::Blue alias + slice-is-additive guard with bumpable consumer-count threshold).
- `/home/headless/Documents/GitHub/designer-agent/` (out-of-repo, read-accessible) — agent-designer workspace. Recent specs in `output/`: `ese-design-spec-2026-05-16.md`, `add-content-modal-spec-2026-05-16.md`, `live-edit-inspiration-from-v2-2026-05-16.md`.
- `Modules/SiteStats/resources/views/filament/pages/site-stats-page.blade.php` — SiteStats admin page (controls widget order — chart above stats cards).
- `Modules/Content/resources/views/filament/admin/empty-state.blade.php` — shared Filament empty state with per-resource CTAs (canonical `.mw-table-empty-cta` insertion pattern).
- `tests/Browser/AdminLiveEditDropdownAndButtonsTest.php` — Dusk test pinning Live Edit toolbar IDs (`#toolbar-user-menu-button`, `#user-menu-wrapper`, `#user-menu nav`, "Dark mode"/"Light mode" text). Consult before deleting any toolbar element.
- `packages/frontend-assets/resources/assets/ui/components/Toolbar/MainDrawer.vue` — Live Edit left drawer (AI-700 / task-2026-05-16-7326d6). Teleport-to-body, role=dialog + aria-modal, 8 nav items, ESC + backdrop close, theme reactivity via `mw.top().admin.theme.on('change')`.
- `packages/frontend-assets/resources/assets/ui/components/Toolbar/PageChip.vue` — Live Edit centre-slot current-page chip + picker popover (AI-701 / task-2026-05-16-66ceca). Outside-click + ESC dismiss via `document.addEventListener('click', handler, true)` capture phase (3rd arg `true`); listener removed in `beforeUnmount`. Consumes MwField token pattern (trivial AI-687 refactor target).
- `packages/frontend-assets/resources/assets/ui/css/index.css` — Live Edit toolbar `:root` token source. `--toolbar-height: 48px` (AI-698a / task-2026-05-16-ec0c87) is the single value that cascades to all toolbar consumers; unrelated `--layouts-dialog-toolbar-height` (60px) and `--toolbar-static-height` (70px) stay separate by design.
- `src/MicroweberPackages/Admin/Filament/FilamentAdminPanelProvider.php` — Filament admin panel provider. Admin path is configurable via `mw_admin_prefix_url()`. AI-702 brand-mark `TOPBAR_START` render hook is registered HERE, BEFORE the existing `admin-top-navigation-actions` Livewire hook so the brand renders leftmost (task-2026-05-16-bcb327).
- `tests/Feature/AdminBcb327AI702BrandMarkContractTest.php` — reference for Filament render-hook ordering contract test (`strpos($source, 'AI-702') < strpos($source, 'admin-top-navigation-actions')`) + admin-logo fallback-chain assertion + every-dynamic-value-escaped-via-`e()` defence-in-depth assertion.
- `tests/Feature/LiveEdit7326d6AI700MainDrawerContractTest.php` — reference for Vue Teleport + ESC handler + N-nav-item wiring + RTL + reduced-motion + 9-token-fallback contract test pattern.
- `tests/Feature/LiveEdit8b10a3AI699SaveButtonPillContractTest.php` — reference for Vue `:class` + `class` split + pulse-animation + reduced-motion + 7-token-fallback contract test pattern.
- `tests/Feature/LiveEdit66cecaAI701PageChipContractTest.php` — reference for outside-click capture-phase + popover + 14-token-fallback + AI-687 MwField refactor-target note contract test pattern.
- `tests/Feature/LiveEditE3da1aAI697AnchoredPickerContractTest.php` — reference for CSS-only Filament-modal anchor + `:has()` scope guard + scope-self-match filter (slice CSS rule lines only) + 4-token-fallback contract test pattern.
- `tests/Feature/LiveEditEc0c87AI698aToolbarHeight48pxContractTest.php` — reference for single-`:root`-token-value-change contract test (with negative-lookbehind regex `(?<![-a-z])--toolbar-height:\s*48px` to avoid false-matching other `*-toolbar-height` tokens).
- `packages/frontend-assets/resources/assets/components/filepicker.js` — image/file picker (`mw.filePicker`) Vue-mounting JS. Tabs initialized via `mw.tabs({ toggle: false })` + `$(scope).trigger('$firstOpen', [section, type])` direct dispatch + `__navigation_first.push(0)` double-fire guard (since AI-770 v2). AI tab outer wrapper class `<div class="mw-image-picker-ai w-full">` (since AI-771). URL tab label "Image URL" + helper text (since AI-772). Tab nav carries `mw-filepicker-component-tab-nav` marker for the AI-768 mobile `:not()` scope guard.
- `packages/frontend-assets/resources/assets/tools/tabs.js` — `mw.tabs()` helper source. Read lines ~87-107 (the click handler) before assuming `trigger("click")` is state-machine-clean on active targets. Default `toggle: true` (active-click toggles off, no `$firstOpen` fires); pass `toggle: false` for deterministic first-open on the initially-active tab.
- `packages/frontend-assets/resources/assets/css/microweber/css/default.css` — frontend-assets master CSS bundle (Vite). `.mw-filepicker-ai-tab-wrap { max-width: none; margin: 0 }` rule lives here (since AI-771); `.mw-filepicker-ai-field-narrow { width: 200px }` secondary-field width constraint also lives here (load-bearing for visual hierarchy).
- `tests/Feature/LiveEdit49ad9dAI770ChangeMwTabsToggleFalseContractTest.php` — reference for `mw.tabs({ toggle: false })` + direct `$(scope).trigger('$firstOpen', ...)` dispatch + `__navigation_first` double-fire guard contract test pattern.
- `tests/Feature/LiveEdit23e0eeAI771AiTabSingleColumnContractTest.php` — reference for two-constraint stack un-wire (outer Tailwind class + inner CSS rule both pinned) contract test pattern, with `.mw-filepicker-ai-field-narrow` 200px preservation guard.
- `src/MicroweberPackages/Filament/Support/AdminDisplayName.php` — canonical CamelCase/snake_case/kebab-case → human-readable normaliser. 25-acronym whitelist constant `ACRONYMS`; static `format(?string $state): string` entry-point. Reference test: `tests/Feature/Admin4905c8AI785AI786R10Slice2ContractTest.php` (DataProvider over 14 transformation cases).
- `src/MicroweberPackages/Filament/Support/AdminFixtureGuard.php` — 158-word Faker vocab + `shouldRenderItem($item)` / `looksLikeFakerLorem($text)` / `filterByTitle($collection, $titleField)` helpers. Used on admin lists pulling from Faker-seeded tables.
- `src/MicroweberPackages/Filament/resources/views/filament-forms/partials/admin-empty-state.blade.php` — reusable `<div class="mw-admin-empty-state">` partial with `__heading` + `__body` + optional `__cta` slot. Consumed by `resources/views/admin/errors/404.blade.php` and Filament resource empty states.
- `resources/views/admin/errors/404.blade.php` — admin-styled 404 page rendered by `src/MicroweberPackages/Frontend/routes/web.php` Route::fallback() admin-prefix branch. Carries `.mw-admin-empty-state__heading` + "Back to dashboard" CTA + `X-Fallback-Message: admin-404` header + `noindex` meta.
- `src/MicroweberPackages/Frontend/routes/web.php` — Route::fallback() with admin-prefix branch that catches `/admin/*` 404s BEFORE the generic FrontendController fallthrough. Branch uses `mw_admin_prefix_url() ?: 'admin'` for prefix detection.
- `Modules/Content/Filament/Admin/ContentResource.php::parentPageSection()` — wizard section that detects `$isPost` from BOTH `$record->content_type` AND `$get('content_type')`. On isPost: TWO sub-Sections (Parent page `skipCategories=true singleSelect=true contentType='page'` + Categories `skipCategories=false singleSelect=false`).
- `Modules/CustomFields/Filament/Admin/ListCustomFields.php:266` — `->descriptions(CustomFieldTypes::class)` MUST stay uncommented in the `RadioDeck` wizard chain. Pinned by `tests/Feature/Admin6027b9AI788DescriptionsWiredToWizardContractTest.php`.
- `Modules/Content/Models/Content.php::createDefaultBlogPage()` — sets `$blogPage->layout_file = 'blog.blade.php'` before save. Pinned by `tests/Feature/Content4e9d1bAI792DefaultBlogPageLayoutContractTest.php`.
- `tests/Feature/Admin4905c8AI785AI786R10Slice2ContractTest.php` — reference for AdminDisplayName DataProvider + Marketplace resource pipe-through-helper assertion + AI-786 CHANGE dark-mode compound-selector regex (`body\.fi-panel-admin\s+\.fi-ta-text-item\.mw-marketplace-card-description\s*\{[^}]*color:\s*var\(--ese-text-muted[^)]*\)\s*!important`).
- `tests/Feature/Admin6027b9AI788DescriptionsWiredToWizardContractTest.php` — reference for Stage-1 consumer-wiring contract test pattern (strip `//` comments before grep-asserting the call IS present + uncommented).
- `tests/Feature/AdminA596d2AI793Admin404PropagationContractTest.php` — reference for branch-ordering source-level invariant assertion (admin-prefix branch sits BEFORE generic fallthrough in Route::fallback) when full HTTP fetch verification is blocked by other catch-all middleware.
- `tests/Feature/LiveEdit255d24AI790XdataEscapeLeakContractTest.php` — reference for Alpine.data() factory-extraction contract test (asserts bare `x-data="addContentModal"` reference + factory registration block + absence of inline `x-data="{...}"` longer than N chars).
- `Modules/Logo/resources/views/templates/default.blade.php` — Logo trilogy ref (AI-803 viewport `@media (max-width: 575px)` gate + AI-804 three-tier alt fallback chain + AI-805 collapsed dead conditional). AI-805a Path B (`is_live_edit() ? '#' : site_url()`) flagged inline as follow-up candidate.
- `tests/Feature/LogoFa5dc3AI803LogoVisibilityContractTest.php` — reference for the `@media`-slice-out + CSS-comment-strip negative regression-guard pattern (Stage-3 viewport-scope-leak family). Asserts `.logo-module { margin: 20px 0; }` body only at top-level scope by pre-stripping CSS `/* */` comments AND slicing the narrow-viewport `@media` block OUT before the absence check.
- `tests/Feature/Logo488fa3AI804LogoAltFallbackContractTest.php` — reference for three-tier WCAG 1.1.1 alt-fallback regex pin (`isset($text) && $text !== '' ? $text : (get_option('website_title', 'website') ?: 'Home')`) + legacy empty-string fallback negative regression guard with Blade-comment pre-strip.
- `tests/Feature/LogoE2e29aAI805DeadBranchCleanupContractTest.php` — reference for "pin the shape not the keyword" guard (asserts specific dead-pattern signature `/@if(...) site_url() @else site_url() @endif/` rather than bare `@else` which legitimately exists in the image/text fallback chain) + AI-805a Path B follow-up marker assertion.
- `src/MicroweberPackages/User/resources/views/layout.blade.php` — AI-794 auth chrome layout. Extends `$extendsView ?? 'templates.bootstrap::layouts.master'` (active-template-master with Bootstrap fallback, AI-757 pattern). Wraps in `.mw-auth-card` with `<header class="mw-auth-header">`, brand logo capped 64×64 via `mw()->ui->admin_logo()`, brand name via `mw()->ui->brand_name()`. Yields `@yield('auth_form')`.
- `tests/Feature/AuthF141ffAI794ForgotPasswordChromeContractTest.php` — auth chrome contract test (active-template extends, `.mw-auth-card`, `mw()->ui->brand_name()` usage + NO bare `brand_name()` regression guard, `@yield('auth_form')` form-slot contract).
- `resources/views/frontend/errors/404.blade.php` — AI-795 frontend 404 renderer. Extends `$extendsView ?? 'templates.bootstrap::layouts.master'`, emits `<meta name="robots" content="noindex,nofollow">`, styled `.mw-frontend-404` card with brand-blue `#0d6efd` CTA.
- `src/MicroweberPackages/App/Http/Controllers/FrontendController.php` — carries AI-795 frontend 404 short-circuit branch inside the `if (!is_admin())` arm (after template-404.php checks). Returns 404 + `X-Robots-Tag: noindex, nofollow` + `X-Fallback-Message: frontend-404` headers.
- `tests/Feature/FrontendC54f1fAI795NotFoundChromeContractTest.php` — frontend 404 contract test (controller branch ordering + view extends-active-template + headers + noindex meta + brand-blue CTA).
- `Modules/Cart/resources/views/page.blade.php` + `Modules/Checkout/resources/views/livewire/cart-items.blade.php` — AI-796 cart empty-state chrome. Cart page gates checkout CTA via `@if($mwAi796HasItems = !empty(mw()->cart_manager->get() ?? []))`. Livewire empty branch renders `.mw-cart-empty` heading + body via Microweber `_e('...', true)` + single "Continue shopping" CTA. Brand-blue `#0d6efd !important` on `.mw-cart-standalone-checkout-cta` + `.mw-cart-empty-cta`.
- `tests/Feature/Cart0e6cfaAI796EmptyStateContractTest.php` — cart empty-state contract test. Includes regression guard against Blade `@if(...)` / `@else` / `@endif` directive tokens appearing in CSS docblock comments (the 2-recurrence Blade-in-CSS-comment footgun) + Microweber `_e('...', true)` usage assertion.
- `packages/frontend-assets/resources/assets/ui/components/Toolbar/MainDrawer.vue` — updated by AI-798/799/800. 3 labeled `<h3 class="mw-main-drawer__section-header">` sections (Edit / Navigate / Preferences). 9 `data-mw-drawer-item="<slug>"` markers. New "Pages" item dispatches `mwOpenPageChip` CustomEvent. `safeRoute(name, fallback)` Ziggy try/catch wrapper + `readMenuUrls()` `#`-placeholder gate. Labels: "Back to admin" / "Templates & layouts" / "Theme settings" / "View public site".
- `tests/Feature/LiveEdit7a9913AI798MainDrawerHierarchyContractTest.php` + `LiveEdit918e58AI799MainDrawerUsersHrefContractTest.php` + `LiveEditF39d53AI800MainDrawerCopyPassContractTest.php` — MainDrawer 3-section + `safeRoute` Ziggy wrapper + label rename contract tests. Reference for the `safeRoute()` try/catch pattern + CustomEvent verb-bridge + `data-mw-drawer-item` slug pinning + DataProvider-pre-Laravel-boot relative-path pattern.
- `Modules/Content/resources/views/templates/{default,skin-1,masonry,dictionary,search,sidebar}.blade.php` — AI-801 content-type fallback inference. `$mwAi780Type` derives from `$params['type']` via `match` when `$params['content_type']` is missing. Parser invariant pinned at `src/MicroweberPackages/App/Utils/ParserLoadModuleTrait.php:405-407`.
- `tests/Feature/CanvasFe8f9eAI801ContentTypeInferenceContractTest.php` — content-type fallback inference contract test (6 template files × `match` shape + parser-invariant docblock ref).
- `packages/frontend-assets/resources/assets/css/scss/liveedit.scss` — AI-802 spacer label hide rules at file tail (`.mw-le-spacer-info-content { opacity: 0; transition: opacity .2s ease }` + 3 show triggers + `prefers-reduced-motion` guard). Source-order tiebreak — must stay AFTER any earlier `.mw-le-spacer-info-content` rule.
- `tests/Feature/CanvasE06decAI802SpacerLabelHideContractTest.php` — spacer label hide contract test (opacity:0 + 3 show triggers + prefers-reduced-motion + source-order tiebreak).
- `tests/Feature/Admin2b1020AI816SaveCtaPrimaryColorContractTest.php` — Slice B audit-the-base-class contract test reference (AI-816). DataProvider over 5 in-scope save CTAs (`CreateContent::saveContent` / `EditContent::EditAction` / `AdminLiveEditPage` in-canvas Create modal / `ContentTableList::CreateAction` + `EditAction`) × (positive `->color('primary')` + negative `->color('success')` regression guard with `//` + `/* */` comment-strip selector-self-match guard); `class_inheritance_carries_fix_to_create_page_and_create_product` (pins `CreatePage extends CreateContent` + `CreateProduct extends CreateContent`); `obsolete_match_the_green_toolbar_docblock_replaced_with_ai816_rationale`; stays-green regression guards (Publish bulk ContentResource:2107 + Place Order CheckoutWizard:451 per AI-209); 16+ AI-816a follow-up candidates documented in test docblock. Uses fixed-length 4000-char `substr` lookahead (NOT slice-by-`;`) because the AdminLiveEditPage docblock contains semicolons in prose.
- `Modules/Content/Filament/Admin/ContentResource/Pages/CreateContent.php` (`saveContent` action) + `Modules/Content/Filament/Admin/ContentResource/Pages/EditContent.php` (`EditAction` Save chain) + `Modules/Content/Filament/ContentTableList.php` (`CreateAction` + `EditAction`) — 5 in-scope AI-816 Save CTA surfaces, all carry `->color('primary')` + AI-816 task-id marker (NOT `->color('success')`). Class inheritance carries the `CreateContent` fix to `CreatePage` + `CreateProduct` via `extends`. Reference: task-2026-05-17-2b1020.
- `packages/microweber-filament-theme/resources/assets/css/microweber-theme-v3.scss` — Webpack-bundled Filament theme master. Carries AI-819 `:root { --primary-50..950 }` RGB-triplet override block (anchored at `MwColors::Blue` values verbatim; slot 500 = `13, 110, 253` = `#0d6efd`) that bypasses Filament v5's `ColorManager` OKLCH emit. New `--primary-*` consumers must compose via `rgba(var(--primary-NNN), <alpha-value>)` (Tailwind v3 shape). Reference: AI-819 / task-9a12c2.
- `Modules/Billing/Filament/Admin/Resources/SubscriptionPlanResource.php` — multi-currency wizard reference. `resolveCurrencyPrefix(?string $currency): string` static helper (6-currency map USD/EUR/GBP/CAD/AUD/JPY + shop-default fallback). Currency Select carries `->live()` so Price + Trial-price prefix updates reactively. Pattern source for any future multi-currency Filament surface. Reference: AI-818 / task-fc0b22.
- `Modules/Invoice/Filament/Resources/InvoiceResource.php` — line-item Price input uses the `currency_symbol() ?: '$'` shop-default shape per AI-818. Reference for invoicing/billing surfaces that aren't multi-currency wizards.
- `Modules/Logo/resources/views/templates/default.blade.php` — full Logo trilogy + AI-803 CHANGE + AI-805a Path B (5 commits on one template): AI-803 viewport @media gate (`fa5dc3`) → AI-803 CHANGE top-level `.logo-link inline-block` parent-flex-collapse fix (`852a8549ba`) → AI-804 three-tier alt fallback (`488fa3`) → AI-805 dead-branch collapse (`e2e29a`) → AI-805a Path B live-edit-disabled navigation (`fb0a119a02`). Anchor href: `is_live_edit() ? 'javascript:void(0)' : site_url()` + conditional `title="Use the menu to navigate" aria-disabled="true"`.
- `src/MicroweberPackages/Filament/resources/views/filament/components/layout/live-edit-module-settings.blade.php` — admin-side `<style>` block hosts AI-691a desktop Cancel-hide + AI-697 anchored picker + AI-817 `.mw-fb-title-wrap` sr-only label rule. New AT-accessible visually-hidden CSS rules belong here when scoped to live-edit module settings forms.
- `tests/Feature/Admin04f015AI817TitleInputLabelContractTest.php` — AI-817 sr-only label-restoration contract (DataProvider over 2 surfaces × `->label('Title')` + `aria-label` + `->hiddenLabel()`-absent regression guard with selector-self-match comment-strip).
- `tests/Feature/Adminfc0b22AI818CurrencyPrefixContractTest.php` — AI-818 currency-aware price prefix contract (DataProvider over 5 surfaces + `ReflectionMethod` unit test on `resolveCurrencyPrefix()` covering 6 currencies).
- `tests/Feature/Admin9a12c2AI819PrimaryColorTokenContractTest.php` — AI-819 Filament v5 OKLCH-vs-Tailwind-RGB-triplet format-mismatch contract (DataProvider over 11 shades + built-bundle runtime probe asserting `:root` block presence after Webpack build).

## Build & Run

### Install
- Requires PHP `^8.3` per `composer.json` (Laravel 11 minimum). Node 18+ for the Vite/Webpack pipelines.
- `composer install` — PHP dependencies.
- `npm install` — root JS dependencies (then per-package `npm install` under `packages/frontend-assets/`, `packages/frontend-assets-libs/`, `packages/microweber-filament-theme/`, `docs/` as needed; the root `package.json` does NOT auto-recurse).
- Copy `.env.example` → `.env`, run `php artisan key:generate`, configure DB credentials, then `php artisan migrate`.

### Development
- `php artisan serve` — local PHP dev server (default `http://127.0.0.1:8000`). Production typically uses Apache/nginx in front of `public/index.php`.
- `npm run dev` (root) — orchestrates per-package dev/watch builds via `run-dev.js`.
- Per-package dev builds (scoped, faster):
  - `cd packages/frontend-assets && npm run dev` (Vite watch).
  - `cd packages/microweber-filament-theme && npm run dev` (Webpack watch).
- VitePress docs preview: `cd docs && npm run docs:dev`.

### Production / one-shot build
- `npm run build` (root) — orchestrates production builds across all packages via `run-build.js`.
- Per-package production builds:
  - `cd packages/frontend-assets && npm run build` (Vite).
  - `cd packages/microweber-filament-theme && npm run build` (Webpack).
- VitePress docs production build: `cd docs && npm run docs:build`. Must stay green; targeted `ignoreDeadLinks` patterns in `docs/.vitepress/config.js` whitelist filesystem-pointer refs (`../../Modules/`, `.php`, etc.) so real dead-link detection still catches new ones.
- After editing anything under `packages/<pkg>/resources/**` (Vue, SCSS, CSS, JS, TS), rebuild that package — the running app serves the built bundle, not source.
- `cd packages/<pkg>` (or any subdir) leaves the shell CWD in that subdir for subsequent commands. For later `git`, `npm`, or tool calls in the same dispatch, prepend `cd /home/headless/Documents/GitHub/microweber && ...` or use absolute paths — otherwise pathspecs fail with `did not match any files`.

### Tests
- `composer test` — main PHPUnit run.
- `./run-tests.sh` — split-process runner that groups suites separately (avoids the ~6 MB/test memory leak).
- `vendor/bin/phpunit --filter=<ClassName>` — run a single test class (fast iteration on contract tests).
- `vendor/bin/phpunit tests/Feature/<File>.php` — run a single file.
- Test DB is MySQL `microweber_testing` (real DB, not sqlite, not mocked); kill any prior PHPUnit run before starting a new one.

### Operational
- `php artisan migrate` — apply core + module migrations.
- `php artisan queue:work` — process background jobs.
- `php artisan schedule:run` — invoked by a system cron each minute.
- Admin password reset for local dev (Apache `/admin/login` 404 quirk gotcha forces this for Playwright runs against `php artisan serve`): `php artisan tinker --execute='\App\Models\User::where("email","admin@example.com")->first()->update(["password"=>bcrypt("admin")])'`. Used 2026-05-16 / task-f4bf24 to onboard agent-designer.

## Gotchas & Known Issues
- The local Apache-served runtime in this environment returns `404` for `/admin/login` even though `http://127.0.0.1` responds with `200`, so browser verification can fail for environment reasons rather than app regressions.
- The full PHP test surface is intentionally split by `run-tests.sh` because long single-process runs hit PHP memory fragmentation/OOM issues (~6 MB/test leak).
- The root repo now documents PHPUnit + `run-tests.sh` as the active entrypoints; the remaining Pest helper files under `docs/testing/` are optional scaffolding only, not live root configuration.
- Live-edit theme helpers must stay scoped to `.mw-admin-live-edit-page`; unscoped Filament tab/input/modal overrides leak into unrelated admin screens and create recurring visual regressions.
- No global `[x-cloak] { display: none }` rule exists. Alpine paints once before init, so any element with text bindings flashes literal `""`. Use inline `style="display: none;"` defaults for empty states instead of relying on `x-cloak`.
- Bash heredoc appends to CSS files have produced reproducible mid-stream duplications. Append via the `Edit` tool with `old_string` = existing file tail, `new_string` = tail + new block.
- `->modalCancelAction(false)` is not the correct Filament API — use `->modalCancelActionLabel('Cancel')`.
- Filament RichEditor renders into `[contenteditable=true]` inside `.fi-fo-rich-editor` / `.ProseMirror`; it does NOT bubble plain `input` events reliably. Listen on `input` + `keyup` + `blur` together when watching for content changes.
- Do not use PHPUnit `RunInSeparateProcess`, `DatabaseTransactions`, or `RefreshDatabase` traits, and never run tests in parallel — the suite is not safe under those modes.
- Window-event handler names registered between admin frame and iframe must match exactly on both sides (`liveEditSaveCallMountedAction`, `liveEditUndoLastPublish`, `liveEditInsertLayoutRequest`). Adding a new verb means editing both files.
- VitePress + Vue interpolation: `{{ expression }}` in markdown prose or inline backticks (outside fenced code blocks) is parsed by Vue at SSR. If the expression isn't a real method on the rendering context, `npm run docs:build` fails with `TypeError: _ctx.<x> is not a function`. Escape with `&#123;&#123; expression }}`. Fenced ```code blocks``` are safe.
- VitePress `ignoreDeadLinks` regex patterns must accept the `./` prefix that nested pages emit — don't anchor with `^`. Whitelist filesystem-pointer paths so real dead-link detection still catches new ones.
- Sitemap previously had an unconditional `return true;` at the top of `needToUpdateSitemap()` that bypassed the 3hr TTL and forced regeneration on every request. Fixed (commit 7386606cd1) and pinned by a contract test that re-fails if the short-circuit returns.
- Filament `Action::make()->color('success')` rendered with `size=lg` (e.g. "Place Order") applies inline styles that beat normal CSS selectors — enforcing `min-height` requires `!important` on `button.fi-btn.fi-color-success`. Discovered AI-517.
- JIRA queue drift: PM's view can run 1–4 cycles behind on-the-ground state when ship cadence exceeds queue-refresh interval. Observed 3+ times in a single session (AI-322..AI-335 tier-1 docs; AI-326 hardening; AI-510..AI-515 Phase 1 UX). Mitigation: status-correction reply with ship-table + commit hashes + JIRA-refresh recommendation, no defensive tone.
- CSS class collision footgun: `.mw-empty-state-cta` is owned by `DashboardEmptyStateWidget` (`microweber-theme-v3.scss`); naming a new button class the same is silently overridden in the cascade. The render appears correct visually until `getComputedStyle` shows the wrong background. Always grep `packages/` for the proposed class name before adopting it.
- Bare `#toolbar { flex-wrap: nowrap }` rule does NOT apply on mobile — `.mw-(admin-)?live-edit-page #toolbar` inside `live-edit-mobile.css` `@media (max-width: 768px), (pointer: coarse)` wins. Mobile-only toolbar fixes must live in `live-edit-mobile.css`.
- Reproducing a UX bug requires the correct viewport. Initial measurement at 1440px during task-74c5f5 showed no overlap; reproducing required resizing Playwright to ~560×440 to match the user's screenshot before the root cause (mobile-specific `flex-wrap: wrap` rule) became visible.
- Live Edit toolbar back-compat hooks asserted by `tests/Browser/AdminLiveEditDropdownAndButtonsTest.php`: `#toolbar-user-menu-button`, `#user-menu-wrapper`, `#user-menu nav`, `.mw-le-hamburger`, "Dark mode"/"Light mode" text. Removing any breaks the Dusk suite. Use `style="display: none;"` to keep the DOM hook while removing the visual rendering.
- `Templates/<X>/` directories (e.g. `Templates/Big2/`) are gitignored at `.gitignore:94` and are **separate git repositories** in their own right. Edits inside `Templates/Big2/` MUST be committed inside `Templates/Big2/`'s own git history — never force-add to microweber via plumbing. Practical effect: a fix that lands only in a gitignored template (per AI-531a) does NOT persist to fresh microweber clones; the long-term solution is to migrate the affected CSS/asset to a non-gitignored location (`packages/frontend-assets`, `Templates/Bootstrap/`).
- Tailwind v4 dual-property `translate` + `transform`: Tailwind v4 utilities like `-translate-x-full` compile to the native CSS `translate: -100% !important` property — NOT `transform: translateX(...)`. Overriding only `transform` in a project rule silently fails because CSS `translate` is a separate property that wins. Symptom: element appears off-screen even though DevTools shows `transform: matrix(1,0,0,1,0,0)`. Fix: reset BOTH properties (`translate: none !important; transform: translateX(...) !important;`). Reference: `src/MicroweberPackages/Filament/.../live-edit.blade.php` `.fi-sidebar` rule (task-2026-05-16-02760c, commit `3d5baca3b1`).
- Alpine `x-data="..."` attribute corruption: `/* … */` block comments inside `x-data` that contain literal `"` characters terminate the HTML attribute at the first embedded `"`, silently corrupting the entire x-data block. Cascade of `q is not defined` / `hasVisibleCardsInGroup is not defined` errors follows. Reference: `add-content-modal.blade.php` regression-guard test `comment_block_uses_single_line_comments_only_in_xdata` (task-2026-05-16-968a71). **Update 2026-05-17 (AI-790):** the same parser quirk hits `//` line comments containing quoted prose (e.g. `// when typing "page" the filter…`) — the in-place "use `//` only" workaround is fragile. Robust fix for any long Alpine x-data: extract to `Alpine.data('factoryName', () => ({...}))` registration in a separate `<script>` block + reference via bare `x-data="factoryName"`. New regression-guard test `LiveEdit255d24AI790XdataEscapeLeakContractTest`.
- Stage-2 cascade-loss dark-mode-only (AI-786 CHANGE / task-2026-05-17-bed599): a single CSS colour rule passing all light-mode contract tests + light-mode browser verification can still render white-on-white in dark mode because Filament's `body.fi-panel-admin.dark .fi-ta-text { color: white }`-style defaults outrank single-class colour selectors via specificity. Source-level tests don't catch it; only dark-mode browser verification does. Fix: split the rule — sizing/layout at single-class specificity, colour at a compound `.fi-X-base.mw-Y-modifier` selector with `!important` on the colour declaration only. Reference: `Admin4905c8AI785AI786R10Slice2ContractTest` `css_color_uses_compound_selector_with_important_dark_mode_fix`.
- Stage-1 data-shipped-consumer-not-wired (AI-788 CHANGE / task-2026-05-17-6027b9): every layer of contract test passes — the data source ships, the DataProvider verifies the values are well-formed, the source-side enum carries actionable copy — but the user sees zero description nodes because the consumer call is `// ->descriptions(...)` (commented out) at the site that mounts the data into the UI (e.g. `ListCustomFields.php:266`). Same defect family as AI-735→AI-793 admin route propagation and AI-771 CHANGE stale theme bundle. Catch only via runtime probe (DOM count / HTTP fetch). Add a consumer-wiring contract test that pre-strips `//` comments before grep-asserting the call IS present + uncommented.
- Long Alpine `x-data="..."` must be extracted to `Alpine.data('factoryName', () => ({...}))` registration once it crosses any of these thresholds: >10 LOC, contains any comments, or contains any embedded `"`. The HTML attribute parser is the limiting factor; extracting to a factory removes attribute-escape concerns entirely. Reference: AI-790 / `add-content-modal.blade.php`.
- PHP docblock prose containing a literal `*/` terminates the docblock prematurely. Cure: rewrite as words (e.g. "slash-star ... star-slash style block comment") rather than reproducing the actual characters. Same family as the x-data quote bug — sequences that have meaning to parsers must not appear in prose inside those parsers.
- Vuetify CSS is NOT loaded in the parent Microweber admin window — only inside iframe/canvas surfaces that bundle it. Historical defensive `!important` fortresses on `.v-slider-*` / `.v-input-*` in `element-style-editor.css` had no real specificity competitor and were safe to drop. Surviving `!important` cases documented inline: `.v-slider-thumb` positioning (Vuetify writes inline styles on drag), hidden a11y input, `.form-control-input-range-slider` fight with `live-edit-input.css`. Reference: `vuetify-slider-in-mw-admin` skill + task-2026-05-16-1b5604.
- Safe attribute-selector interpolation inside an HTML attribute inside a JS expression: use `JSON.stringify(value)` rather than string concatenation with double-quoted literals. Example: `querySelectorAll('[data-mw-add-content-group=' + JSON.stringify(group) + ']')` — keeps every escape layer correct (HTML attribute → JS expression → CSS attribute-selector value). Concatenating raw double-quotes requires triple-escaped quotes and is exactly the breakage pattern that killed the first attempt at AI-692.
- Filament render-hook ordering: hooks registered at the same name (`PanelsRenderHook::TOPBAR_START`, etc.) accumulate in registration order — FIRST-registered renders leftmost. Surprising mental model for agents who expect "last registered wins". Assert ordering in contract tests via `strpos($source, 'MARKER_A') < strpos($source, 'MARKER_B')`. Reference: AI-702 brand-mark hook in `FilamentAdminPanelProvider.php`.
- Vue Teleport / Filament-portaled DOM token scope: `<Teleport to="body">` and Filament modal/popup containers render OUTSIDE the typical `.mw-live-edit-page` wrapper. Locally-scoped tokens (`.mw-live-edit-page { --x: ... }`) do NOT resolve in that portaled DOM — `getComputedStyle` returns `unset`. Tokens consumed there MUST be `:root`-scoped, and every `var(--token, fallback)` MUST carry a literal fallback as belt-and-braces. Surfaced repeatedly in AI-697 anchored picker, AI-700 MainDrawer, AI-701 PageChip — all consume `:root` ESE tokens.
- Vue popover outside-click dismiss handlers MUST register on `document` with capture phase (`addEventListener('click', handler, true)`, 3rd arg `true`). Default bubble phase loses to inner `stopPropagation()` calls (Filament/Livewire/Alpine all use this internally for their own modals). Always remove the listener in `beforeUnmount` / `beforeDestroy` to avoid leaks. Reference: AI-701 PageChip popover dismiss.
- Vue `:class` + `class` attribute split for state-class triads: combining static legacy classes (`'btn btn-dark live-edit-toolbar-buttons'`) with dynamic state classes (`'mw-save-button--idle': !_dirty`) into a single `:class` map makes the asserted class string include legacy noise — contract tests then false-fail when pinning bare `'mw-save-button': true`. Always split: legacy classes stay on the `class="..."` attribute (HTML), state classes go on `:class` (Vue). Reference: AI-699 `SaveButton.vue` rework.
- Pulse-animation pattern for periodic accent indicator (non-overwhelming): `animation: name 60s linear infinite` where keyframes flash the indicator in the first ~2% of each cycle (`0% { box-shadow: 0 0 0 0 transparent; } 1% { box-shadow: 0 0 0 4px var(--ese-accent); } 2%, 100% { box-shadow: 0 0 0 0 transparent; }`) — perceived as a periodic pulse, never annoying. Always pair with `@media (prefers-reduced-motion: reduce) { animation: none; }`.
- Anchored Filament modal pattern (no PHP/DOM changes): tag the rendered `.fi-modal-window` via `Action::extraModalWindowAttributes(['class' => 'mw-X'])`, then add admin-side `<style>` overrides scoped via `:has(> .fi-modal-window-ctn .mw-X)` on the outer `.fi-modal` wrapper. The `:has()` scope keeps other modals at their centered + dimmed defaults. Reference: AI-697 anchored picker.
- `e()`-escape every dynamic value emitted from a Filament render hook (defence-in-depth): even when source values are config-derived and "low-trust internal", wrap every interpolation in `e()` so any future config-injection vector can't corrupt the topbar/admin chrome HTML. Reference: AI-702 brand-mark hook escapes `$logoUrl` / `$brandName` / `$adminUrl`.
- Single-source-of-truth check before shipping a new state indicator: grep across `packages/frontend-assets/` + `src/MicroweberPackages/LiveEdit/` for redundant Saved/Unsaved/dirty badges BEFORE adding a new one. If a redundant indicator already exists, remove or repurpose it — don't pile up new state surfaces. Reference: AI-699 dirty-state pre-ship grep found zero redundant badges (clean ship).
- 3 pre-existing test failures in `tests/Unit/Template/AddContentModalContractTest.php` (actions-array count 6 vs expected 5; `addToCurrentPageAction` lookup; one related action-order assertion). Confirmed via `git stash` round-trip 2026-05-14 / task-dac0b8 that they exist on parent commits — unrelated to current session work. Flagged for a separate PHP-controller-side cleanup pass; don't try to fix as drive-by during chrome arc.
- Memory-MCP back-compat: 3 file-backed memory snippets that other agents may consume — `feedback_heredoc_append.md` (never heredoc CSS appends; use Edit), `feedback_always_build.md` (always rebuild after `packages/<pkg>/resources/**` edits), `feedback_templates_subrepo.md` (`Templates/<X>/` directories are separate git repos; edits don't persist to microweber). Indexed at `/home/headless/.claude/projects/-home-headless-Documents-GitHub-microweber/memory/MEMORY.md`.
- `mw.tabs()` active-tab `trigger("click")` footgun: programmatic `$target.trigger("click")` on an already-active `mw.tabs` tab enters the toggle-OFF branch (removes `.active`, hides content) and `$firstOpen` does NOT fire. Symptom: lazy-mount on the default-active tab never happens, iframe stays empty, the user-visible feature is missing. Either pass `toggle: false` to mw.tabs init, OR fire `$(scope).trigger('$firstOpen', [section, type])` directly + push the index to `__navigation_first` as a double-fire guard. Diagnosed by designer's runtime probe of mw.tabs's actual click handler (AI-770 v1 regression / task-2026-05-17-49ad9d).
- Shared CSS class hide without `:not(.marker)` scope guard: `.mw-live-edit-resolutions-wrapper` is used by BOTH the canvas resolution switcher AND the filepicker tab nav. A bare mobile `display: none !important` rule kills both — picker tab nav disappears at <576px. Add a unique marker class to the keep-visible surface FIRST, then rewrite the suppression rule as `.shared-class:not(.marker) { display: none !important }` (AI-768 / task-2026-05-17-2c33e0).
- Two-constraint stack (Tailwind outer + CSS inner): when both a Tailwind utility on the outer wrapper AND a CSS rule on an inner element constrain the same dimension, fixing only one leaves the other winning the cascade. AI-771's AI-tab had `max-w-sm mx-auto` (Tailwind ~384px) + `.mw-filepicker-ai-tab-wrap { max-width: 400px }` (CSS 400px) both pinning ≈400px. Inspect computed style at BOTH levels before declaring a layout constraint released.
- Contract-test selector-self-match guard (recurring 11+ times this session — AI-518/522/531/691a/697/710/711/712/714/715/716/717/718/719): when a PHPUnit contract test asserts the ABSENCE of a string (`assertStringNotContainsString` / `assertDoesNotMatchRegularExpression`) on a file that ALSO carries the docblock or prose describing what was removed, the assertion self-matches on its own documentation and false-fails. Always slice the assertion to the CSS rule body / function body only — `strpos` from the unique marker (e.g. `/* ── AI-NNN`) to the next AI-block marker (or `}` for single-rule scope). Skip Blade `{{-- … --}}` and JS `/* */` `//` comments via `preg_replace` before grepping. Pattern is now 6th-occurrence-stable: preemptive slice-bounding is faster than first-run-iterate-on-failure.
- Two-stage CSS shipping verification (source-level + runtime-probe, established post-task-bc28fd CHANGE): source-level contract tests pin source PRESENCE, not runtime DELIVERY. A source-level test scanning the wrong file (or scanning a Blade `<style>` that isn't loaded on the target page) passes happily while the live build ships without the rule. Pair every CSS rule shipping into a Webpack-bundled live-edit surface with a runtime-probe test that reads the SERVED bundle (`public/vendor/.../microweber-filament-theme.css`) directly + asserts bundle `mtime ≥ source mtime`. Reference: `LiveEditBc28fdAI691a697ServedBundleRuntimeProbeContractTest`.
- Stage-2 cascade-loss dark-mode-only (AI-786 CHANGE / task-2026-05-17-bed599): a single CSS rule passes light-mode contract + browser tests but resolves white-on-white in dark mode because Filament's `body.fi-panel-admin.dark .fi-ta-text { color: white }`-style defaults outrank single-class colour selectors via specificity. Catch only via dark-mode browser verification — source tests are blind to it. Fix shape: split the original rule into TWO — sizing/layout stays at single-class specificity, colour moves to a compound `.fi-X-base.mw-Y-modifier` selector with `!important` on the colour declaration only.
- Stage-1 data-shipped-consumer-not-wired (AI-788 CHANGE / task-2026-05-17-6027b9 + AI-771 CHANGE + AI-735→793): source-of-truth ships + DataProvider tests + contract tests all green, but the user never sees the new data because the consumer call site is `// ->descriptions(...)` (commented out at `ListCustomFields.php:266`) OR the bundle that ships the source data is stale OR the renderer that consumes the new route ignores the new branch. Catch only via runtime probe (DOM check / HTTP fetch). Diagnostic signature: source layer correct + tests green + consumer site has the wiring commented OR uses a default that doesn't reach the new data.
- Stage-5 template-attribute-escape-leak extends beyond `/* */` block comments (AI-790 / task-2026-05-17-255d24): `//` line comments inside long Alpine `x-data="..."` ALSO corrupt the attribute when they contain literal `"` characters — the HTML attribute parser still terminates at the first embedded `"` regardless of comment style. The robust fix is to extract any long Alpine x-data (>10 LOC OR contains any comments OR contains any embedded `"`) to `Alpine.data('factoryName', () => ({...}))` registration in a separate `<script>` block + reference via bare `x-data="factoryName"`.
- **Blade `@<directive>` tokens inside CSS docblock comments embedded in `<style>` blocks crash the Blade compiler** — 2 session-recurrences 2026-05-17 (AI-796 cart empty-state docblock with `@else` prose; AI-803 Logo SCSS docblock with literal `@if(is_live_edit())` prose) — both crashed the live page with "syntax error, unexpected end of file, expecting elseif/else/endif". The Blade compiler parses `@if(` / `@else` / `@elseif` / `@endif` tokens as directives even inside `/* ... */` CSS comments. Symptom: 500 on the affected page; light-mode/dark-mode/contract tests don't catch it (compile-stage failure). Rephrase prose to remove the literal directive token ("else-branch" instead of "@else branch"; "the if-statement that gates editor mode" instead of "@if(is_live_edit())"). Same parser-meaningful-character family as Alpine `x-data` `"` corruption + PHP docblock `*/` rule + VitePress `{{ … }}` SSR collision.
- Stage-3 viewport-scope leak sub-case (AI-803 / task-2026-05-17-fa5dc3): a CSS rule designed for a specific viewport but declared OUTSIDE its intended `@media` block applies to ALL viewports. Source-only tests pass; only cross-viewport rendered-browser probe catches it. Pre-fix `.logo-module { min-width: 0; overflow: hidden }` was declared unconditionally despite a docblock saying "narrow viewports only" — parent flex column collapsed to 0×0 at every viewport, SVG logo invisible. Fix shape: gate inside `@media (max-width: 575px)`. Contract tests targeting top-level rules MUST slice the `@media (max-width: NNNpx) { ... }` block OUT before asserting absence of narrow-viewport declarations at top-level scope.
- `mw()->ui->brand_name()` is canonical — never call bare `brand_name()` (no such global function; throws "Call to undefined function brand_name()"). Same for `mw()->ui->admin_logo()` / `mw()->ui->admin_logo_login()` per AI-702 brand-mark fallback chain. Caught during AI-794 ship.
- Laravel `__()` returns empty for translatable strings ending in `.` — Laravel treats trailing `.` as translation-key namespace separator (`__('Browse our products and add items.')` → `""`). Use Microweber's `_e('...', true)` (translatable + echo) helper instead. Caught during AI-796 cart empty-state ship.
- PHPUnit `#[DataProvider]` methods run pre-Laravel-boot — calling `base_path()` / Laravel helpers from inside a DataProvider crashes with "Call to undefined method Illuminate\\Container\\Container::basePath()". Pattern: return RELATIVE paths from the DataProvider, prepend `base_path()` per-test via a helper. References: AI-799 + AI-801 contract tests.
- `preg_replace` delimiter `!` collides with HTML `<!--` — `preg_replace('!<!--[\s\S]*?-->!', ...)` fails with PHP warning "Unknown modifier '-'" because `!<` parses as delimiter + modifier flags. Switch to `~` delimiter (or `#`) when stripping HTML comments: `preg_replace('~<!--[\s\S]*?-->~', ...)`. Caught during AI-799 contract test.
- "Pin the shape, not the keyword" (AI-805 / task-e2e29a) — when grep-asserting absence of a legacy pattern, anchor on the SPECIFIC dead-pattern SIGNATURE rather than a directive keyword in isolation. Example: AI-805 asserts `/@if\([^)]*\)[\s\S]*?site_url\(\)[\s\S]*?@else[\s\S]*?site_url\(\)[\s\S]*?@endif/` (both arms return same value — the dead-branch signature) instead of bare `@else` (which legitimately exists elsewhere in the template). Cousin of the selector-self-match guard family; both fail when the assertion is too narrow OR too broad relative to what makes the defect specific.
- Empty alt-fallback violates WCAG 2.1 SC 1.1.1 — `alt="{{ isset($text) ? $text : '' }}"` renders empty alt when no module text override is set, leaving screen-reader users with nothing on the brand mark. Use a three-tier fallback chain: `$text → get_option('website_title', 'website') → 'Home'`. Reference: AI-804 logo alt fix.
- **Save CTA semantic-color footgun**: `->color('success')` (green `#2FB344`) on a Save / Save & Continue / Create UNSUBMITTED-form CTA is a semantic-color regression — green is reserved for post-submit semantic-success states (saved, published, approved, mark-paid, marketplace install/enable). Designer framing: green on the unsubmitted CTA conflates "click to save" with "saved". Use `->color('primary')` (brand-blue `#0d6efd` / `Color::Primary` / `MwColors::Blue`) instead. Reference: AI-816 / task-2b1020. Out-of-scope-stays-green surfaces: Place Order CheckoutWizard (per AI-209 pin), Publish bulk action ContentResource, Mark-Paid OrderResource, Marketplace Install/Enable, widget chart tokens.
- **Obsolete "match the green main-toolbar SAVE pill" rationale**: any source docblock citing the green-match argument as a Save-CTA-color justification is stale — the live-edit toolbar SAVE became a BLACK pill in AI-699 (task-2026-05-16-8b10a3) ending the green-match contract. Replace any such docblock with AI-816's primary-vs-success semantic-color rationale when flipping the CTA. 5 in-scope sources audited 2026-05-17 (CreateContent / EditContent / ContentTableList × 2 / AdminLiveEditPage).
- **Contract-test slice-by-`;` footgun**: bounding a localised assertion on `strpos($source, ';', $anchorPos)` breaks when surrounding docblock prose contains semicolons (e.g. `"lineage; green is reserved..."`) — the slice truncates BEFORE the asserted code and the test false-fails. Use fixed-length `substr($source, $anchorPos, 4000)` lookahead instead (1500 for tight regression-guards, 4000 for full action-chain slices). Pair with the comment-strip selector-self-match guard before negative assertions. Caught during AI-816 contract test on AdminLiveEditPage (task-2026-05-17-2b1020). Same parser-meaningful-character family as the broader LESSONS slice-bounding pattern.
- **Slice B audit-the-base-class** discipline: when a defect family appears to recur across sibling resources (Post / Page / Product / Order / etc.), read the inheritance chain BEFORE shipping per-resource fixes. AI-816 case: `CreateProduct extends CreateContent` + `CreatePage extends CreateContent`, so flipping `CreateContent::getHeaderActions()` once carried the Save-CTA fix to all three resources. The 5 in-scope AI-816 surfaces all shared the same obsolete "match the green toolbar SAVE pill" rationale — recognising the shared origin avoided shipping 5 independent edits.
- **`display: none` on a form-input label removes the element from the AT tree** (WCAG 3.3.2 Level A failure) — screen-reader users lose the label entirely. Filament's `->hiddenLabel()` has the same defect; replace with `->label('Title')` + `->extraFieldWrapperAttributes(['class' => 'mw-X-wrap'])` + sr-only CSS rule (`position: absolute; width: 1px; height: 1px; overflow: hidden; clip: rect(0,0,0,0);`). Reference: AI-817 / task-04f015.
- **Hardcoded `'$'` in Filament price `->prefix('$')` breaks every non-USD shop** — sites in EUR/GBP/CAD/AUD/JPY render the wrong sigil. Use `->prefix(fn () => currency_symbol() ?: '$')` for shop-default surfaces; `->prefix(fn (Forms\Get $get) => static::resolveCurrencyPrefix($get('currency')))` for multi-currency wizards with a `Select::make('currency')->live()`. Canonical helper is `currency_symbol()` (Shop module wrapper) — `option_get('currency_symbol', 'shop')` referenced in some designer dispatches is the back-compat alias, not the documented name. Reference: AI-818 / task-fc0b22.
- **Filament v5 `--primary-NNN` tokens are NOT emitted in Tailwind v3 RGB-triplet format** — `ColorManager::convertToOklch()` emits `12.34% 0.123 45.67` (OKLCH-composable); project's Tailwind v3 consumer `rgba(var(--primary-500), <alpha-value>)` silently falls back to Filament's built-in default blue (`#4299E1`) instead of brand `#0d6efd`. New Stage candidate "framework-emit-format-mismatch". Fix: explicit `:root { --primary-50..950: R, G, B; }` override block at the top of `microweber-theme-v3.scss` anchored at `MwColors::Blue`. Diagnostic-surface heuristic: AI-816 painted most CTAs primary-blue, so Image-add (lacking AI-816) was the surface where `fi-color-primary` still surfaced the underlying defect. Reference: AI-819 / task-9a12c2.
- **No-pre-reservation policy for Jira ticket numbers** (designer policy as of 2026-05-17 after AI-829/AI-830/AI-833 collision chain) — Jira auto-assignment can collide with numbers reserved in design briefs / SHIP reports. File the ticket first, accept the auto-assigned number, reference going forward. Never pre-reserve.
- **Sidebar/menu IA-refactor pattern now 3-recurrence** (AI-798 MainDrawer hierarchy / AI-826 Tools / AI-836 admin sidebar grouping). When a label or grouping change ships at one surface, audit every other surface emitting the same string — the sweep cascade is real, captured by AI-800a (`RightSidebar.vue` + `bootstrap.js` controlBox title).
- **`@if(is_live_edit())` keyword can legitimately re-appear for a different purpose** — AI-805 absence-asserted the bare-keyword `@if(is_live_edit())` (the dead dual-arm conditional), but AI-805a re-introduces the same keyword for conditional attribute emission (the `title="..." aria-disabled="true"` block). Contract tests must anchor on the SPECIFIC dead-pattern SIGNATURE (`@if(...) site_url() @else site_url() @endif` — both arms returning the same value), not the bare directive keyword. "Pin the shape, not the keyword" (AI-805 sub-rule of selector-self-match guard family).
- **Parser-meaningful-character family — unified meta-rule (4-recurrence promotion 2026-05-17)**: any character or sequence with meaning to ANY upstream parser MUST be escaped or rephrased even inside an apparently-inert context (`//` line comments, `/* */` block comments, CSS comments inside `<style>`, docblock prose, HTML attribute values, markdown prose, JS template literals). Full family roster: (1) Alpine `x-data="..."` embedded `"` terminates the HTML attribute (AI-692 + AI-790); (2) PHP docblock prose literal `*/` terminates the docblock (AI-790); (3) Blade `@<directive>` tokens inside CSS docblock comments parse as Blade directives even inside `/* */` CSS comments (AI-796 + AI-803); (4) PHP `?>` literal inside a `//` line comment exits PHP code mode (AI-806); (bonus) VitePress `{{ }}` in markdown prose parses at SSR (AI-326). Canonical fix shape: **rephrase to remove the literal token from prose** (e.g. "the legacy php-print pattern" not the literal `?>`; "slash-star-style block comment" not `*/`; "the else-branch" not `@else`). Comments are LEXER-level — parser-meaningful characters terminate/alter their enclosing structure BEFORE the comment is recognised.

## Decisions
- Cache deserialization in `TaggableFileStore` is hardened with `unserialize(..., ['allowed_classes' => false])` to avoid object-injection risk from poisoned cache files.
- Treat `phpunit.xml` + `run-tests.sh` as the authoritative test entrypoints for the repository root.
- Keep live-edit-specific Filament overrides scoped to the live-edit wrapper instead of styling raw `.fi-*` selectors globally in the admin theme bundle.
- The window-event `CustomEvent` bridge is the canonical communication channel between the Live Edit admin frame and the canvas iframe. New verbs are added by registering matching listeners on both surfaces.
- Primary brand blue is Bootstrap `#0d6efd` across admin + checkout + public, sourced from `MwColors::Blue`. Filament Color enum direct use is avoided so the single token can be re-anchored project-wide.
- Touch-target floor is 44×44 px (WCAG 2.5.5) for all interactive elements. Mobile-only adjustments live in `mobile-touch.css`.
- Length caps for cross-surface query-string carry-forward (Live Edit → Filament Create page): `title` 256 chars, `content_body` 6 KB, `description` 2 KB. Apply caps on both producer (JS) and consumer (`getInitialData()`) sides as defence-in-depth.
- `protected function getInitialData(): array` is the canonical hook for pre-filling Filament Create pages from URL query strings. Do not modify the form schema for this.
- Bounded slice + contract test is the dominant winning pattern. Avoid speculative mechanical refactors without RTL/test content or an explicit dispatch.
- VitePress sidebar entries in `docs/.vitepress/config.js` MUST be appended whenever new module docs land — the docs site does not auto-discover.
- Tier-1 narrative docs (`docs/modules/<name>/`) are the canonical guides; the auto-generated `docs/modules/README.md` filesystem index is a snapshot reference, not the primary entry. Preserve the auto-generated section below the manual landing tables — never delete it.
- Contract tests for helper traits use the anonymous-class pattern: `new class { use TheTrait; };`. Gives a no-DB, no-Laravel-boot unit-test surface — reference `tests/Feature/SitemapCacheTtlContractTest.php`.
- Pure-ack PM emails are never replied to (per `.autodev/CONTRACTS.md` routing). Even celebratory or milestone-marking acks — no ack-of-ack chain.
- Phase 1 UX baseline (AI-510..AI-515) shipped 6 commits, 121 PHPUnit tests, 489 assertions, ~1700 lines of CSS+JS+tests. Deferred follow-ups (AI-510a/b/c, AI-511a/b/c, AI-512a/b/c, AI-513a/b, AI-514a) are documented in CSS comments and await explicit dispatch.
- Helper-layer security principles (ADR-0001): two-pass escape; URL protocol allowlist (`http`/`https`/`mailto:`/`tel:` + `data:image/*` for `<img>` only — never `javascript:` / `data:text/html` / `vbscript:` / `file://`); sanitizers fail closed; one helper per output context.
- Sitemap cron strategy: project ships no default schedule. Three documented patterns — Laravel scheduler (`schedule:run` hourly), direct system cron, event-driven invalidation on content save — see `docs/modules/sitemap/usage.md`.
- CSS-token chain is the canonical unification surface (`--color-product-title` → `--color-text-primary`). Override at the token; avoid per-template patches that drift.
- Filament checkbox 44×44 hit-area uses the cross-browser `:has(.fi-checkbox-input)` pattern on BOTH `.fi-fo-field-wrp` AND `label`. Pattern verified across AI-517 (checkout) and AI-518 (profile) panels.
- Coverage-analysis regression-guard pattern: when recon shows a feature inherits existing CSS coverage, ship a contract test that pins the inheritance chain (no CSS changes) and flag any partial-coverage gap as `AI-NNNa follow-up candidate` in the docblock. Reference: AI-519 Coupon ships `Ai519CouponCoverageContractTest` with no CSS, flagging Surface 4 (admin form min-height) as follow-up.
- Mobile-audit fix surfaces are determined by who owns the rendered HTML — public-template HTML routes to `Templates/Bootstrap/.../public-touch.css` (Vite); Filament-panel HTML routes to `packages/microweber-filament-theme/.../mobile-touch.css` (Webpack). A single audit (e.g. AI-518 Customer) may need rules in BOTH files.
- Commerce-mobile arc (Batch 1 P1+P2 commerce tickets): AI-516 (`3157870616`), AI-517 (`81ffb093fb`), AI-518 (`a9f8dd7a1c`), AI-519 (`a188cdcbfe`) shipped 2026-05-14 — 25 PHPUnit tests / 91 assertions green across `PublicTouchTargetContractTest`, `Ai517CheckoutTouchTargetContractTest`, `Ai518CustomerTouchTargetContractTest`, `Ai519CouponCoverageContractTest`. AI-520 Shipping + AI-521 Payment queued. Deferred: AI-519a (admin form min-height gap) pending tester measurements.
- Live Edit mobile layout: `x-init="..."` attributes must use ONLY single-quoted string literals internally — any `"double quote"` inside terminates the HTML attribute at the first `"`. A misplaced JS comment containing `<form wire:submit...>` became real DOM ~460px tall, pushing the Vue toolbar completely off-screen. Rule: all JS strings inside `x-init="..."` must be single-quoted.
- `padding-top` on `fi-main-ctn` can fight with fixed `top: var(--toolbar-height)` on the canvas `#live-edit-frame-holder` on mobile. On mobile live-edit, there is no Filament fixed topbar — the 56px `padding-top` that was added in cycle-86 (AI-116/TICKET-CI) to guard against topbar overlap instead created a 56px dead zone AND 52px toolbar/canvas overlap. The correct geometry: single-row toolbar (`max-height: 56px`) + canvas at `top: 60px` (4px gap) — no `fi-main-ctn` padding needed.
- Filament `TextColumn` unknown-value normalization: `->formatStateUsing(fn ($s) => $s && strtolower((string) $s) !== 'unknown' ? $s : 'Unknown')` — coerce null/empty/literal "unknown" to a canonical display string. Pair with query-level junk filters (`->where('column', '!=', '0')->whereRaw('CHAR_LENGTH(column) >= 2')`) where appropriate. Reference: `Modules/SiteStats/Filament/Widgets/LocationsWidget.php` (task-2026-05-16-321ef4).
- Dark/light theme sync from a Vue component uses `MutationObserver` on `document.documentElement` with `attributeFilter: ['class']` — Filament toggles the `dark` class on `<html>`. Detach in `beforeDestroy()` / `unmounted()`. Reference: `ToolbarToolsDropdown.vue` (task-2026-05-16-3ae87c).
- Live Edit Save button label is just "Save" (`<span>Save</span>` + `aria-label="Save (Ctrl+S)"` + `title="Save (Ctrl+S)"`) — never "Save & Publish". On the Live Edit surface, saving IS the publish step (there is no separate publish action); click handler `save()` unchanged. Reference: `SaveButton.vue` (task-2026-05-16-3a464f).
- Mobile horizontal-scroll toolbar pattern: `flex-wrap: nowrap !important` + `overflow-x: auto !important` on container + `flex-shrink: 0` on each child. Optionally hide scrollbar via `::-webkit-scrollbar { display: none; }` + `scrollbar-width: none;`. Reference: `live-edit-mobile.css` (task-2026-05-16-74c5f5).
- Mobile vertical-sidebar caption-wrap pattern (Live Edit right rail): `::after { content: attr(aria-label); white-space: normal; overflow-wrap: break-word; max-width: 72px; line-height: 1.15; font-size: 0.6875rem; }` — NOT `nowrap` + ellipsis. Widen parent sidebar + button widths to match.
- Resource list empty-state CTA shape: `<div class="text-center mw-table-empty-cta-wrap"><a class="mw-table-empty-cta" href="{{ route('filament.admin.resources.<plural>.create') }}" aria-label="Add X">+ Add X</a></div>`. Style lives in `general-styles.css` (44px min-height, primary `#0d6efd` blue, full hover/focus/active states). Reference: `Modules/Content/resources/views/filament/admin/empty-state.blade.php` (task-2026-05-16-fd0d1d).
- Add-Content card description-as-tooltip (AI-691 / task-2026-05-16-cdeefd): card description is NOT rendered as a visible body div under the title — kept only in `title="{{ description }}"` (browser tooltip) + `aria-label` (accessibility tree). Card titles drop the "New" prefix ("New Page" → "Page" etc.); the lone exception is "Add a block to this page" (verbatim) — kept as the only primary-group title carrying a verb to distinguish inline-insertion intent from navigate-to-form intent.
- Cancel button on the Add-Content modal stays on desktop until the mobile bottom-sheet (AI-695) lands or a designer-dispatched CSS-scoped desktop-only path. NOVICE #3 (task-2026-05-13-899d57) keeps it for mobile thumb-reach; deleting on desktop alone re-introduces the mobile-stuck-modal regression. Spec §A6 deletion is parked, not abandoned — flagged in `AddContentCdeefdAI691Slice1ContractTest` (the Cancel-kept assertion flips when designer resolves §A6).
- ESE Phase 1 status (5 of 7 slices shipped, 2026-05-16): 1.1 design tokens (`d8cf5a7bc2`), 1.2 MwSlider (`d6896bb409`), 1.3a MwToolButton + MwSegmented primitives + Typography proof-of-pattern consumer (`8649882183`), 1.3b Spacing + Align consumer migrations + AI-693 monochrome icon palette (`5fddf75eff`), 1.7 colour-alias migration (`e25388369c`). Pending: 1.4 MwField row (AI-687), 1.5 empty state (AI-688), 1.6 mobile bottom-sheet (AI-685). AI-684 MwToolButton closable; AI-686 / AI-690 transitioned to Done.
- Add-Content Phase 1 status (5 of 6 slices shipped, 2026-05-16): AI-691 strip "New" prefix + description as tooltip (`636b48799c`), AI-691a Path B desktop-only Cancel hide via CSS (`d910ea0079`), AI-692 two-group layout (`18a18dfadf`), AI-693 monochrome icon palette + accent hover (bundled with ESE 1.3b at `5fddf75eff`), AI-694 search promotion + ⌘K chip + zero-match fallback (`cb3dca473d`). Pending: AI-695 mobile bottom-sheet (blocked on no dep), AI-696 in-modal picker (blocked on AI-695).
- Live Edit chrome refresh arc (v2-inspired, 2026-05-16): AI-708 heading disambiguation (`3617481ee2`) → AI-709 Layers panel rich labels (`7e94de5874`) → AI-697 anchored Add-Content picker (`abfc366d66`) → AI-698a toolbar height 48px (`82f635e91b`) → AI-699 SAVE pill (`61045ae589`) → AI-700 MainDrawer + AI-698b item 3 hamburger (`5417e51a88`) → AI-701 PageChip (`d18570e01f`). Admin shell arc started 2026-05-16: AI-702 brand mark (`441050920e`). Pending live-edit polish: AI-710 Design icon rename, AI-711 Quick AI Edit per-section labels, AI-712 Template Settings panel unify (chained on ESE 1.4 / AI-687), AI-698c (MwSegmented device-preview + ⋯ Tools popover). Pending admin shell: AI-703 responsive sidebar, AI-704 right-cluster (consumes AI-699 pill token contract), AI-705 welcome counter, AI-706 stat hierarchy.
- Designer working contract evolution (2026-05-16): SOUL #108 verify-before-accept (designer browser-verifies every `[SHIP]` at desktop + 390 mobile BEFORE adding DESIGN_AUDIT post-implementation row / closing JIRA / sending `[ACK]`; issues surface as `[CHANGE]` with screenshot evidence, not `[ACK]`) + SOUL #115 per-ticket-email (every Jira ticket gets its own dedicated `[task] AI-NNN — <one-line>` email; subject + URL + spec ref + problem + fix + dependency, <250 words; Jira description holds full detail). Designer also adopted spec-doc-nit: when a token is consumer-scoped rather than `:root`-scoped, SHIP report should call it out so future agents reading the spec aren't confused.
- AI-771 image picker AI-tab: shipped designer Option A (single-column collapse) over Option B (preview-pane filling with the generated result). Option B is a feature ticket — pulls in state wiring between Generate-success callback and a new preview slot; sequencing-cleaner to ship layout collapse first, file preview as a separate ticket AFTER (designer dispatch task-2026-05-17-23e0ee).
- AI-770 v2 CHANGE absorption pattern (pin-evolution): when designer raises a runtime regression on a recently-shipped fix, UPDATE the existing contract-test pin in place (rename + reshape assertion + docblock cross-ref to prior task-ID) rather than adding a parallel v2 test — parallel tests create contradictory contracts and obscure the single-source-of-truth. Codified in CLAUDE.md / AGENTS.md / copilot-instructions.md "Code Style".
- AI-769a follow-up filed (not shipped speculatively): extending the `mw.dialog` dark-skin's input selector list to also cover `.form-control-live-edit-input` waits for explicit designer dispatch per the JOURNAL row 13 risk pattern. Designer noted the gap in the AI-772 ACK; will close on the next dispatch cycle.
- 5-stage failure-family taxonomy (2026-05-17) is the shared vocabulary for ship-report root-cause attribution: Stage-1 (data-shipped-consumer-not-wired), Stage-2 (cascade-loss dark-mode-only), Stage-3 (wrong-surface-mount including propagation-without-renderer-update), Stage-4 (handler-loop-from-generic-helper-default), Stage-5 (template-attribute-escape-leak). Name the stage in CHANGE replies + contract-test docblocks so reviewers know which failure family applies. Designer adopted the phrasing 2026-05-17 / task-db748d.
- Long Alpine `x-data` (>10 LOC OR comments OR embedded `"`) extracted to `Alpine.data('factoryName', () => ({...}))` registration in a separate `<script>` block + bare `x-data="factoryName"` reference is the canonical safe shape — avoids the Stage-5 template-attribute-escape-leak family regardless of comment style. Reference: AI-790 / task-2026-05-17-255d24 (extends the AI-692 `/* */` lesson to `//` line comments).
- Dark-mode cascade-loss fix shape: split a single sizing+colour rule into TWO — sizing/layout at the original single-class specificity, colour at a compound `.fi-X-base.mw-Y-modifier` selector with `!important` on the colour declaration only. Reference: AI-786 CHANGE / task-2026-05-17-bed599 split of `.mw-marketplace-card-description`.
- Stage-1 consumer-wiring contract test pattern: source-side data layer + DataProvider PHPUnit tests are NOT sufficient on their own. Always pair with a consumer-wiring contract test that pre-strips `//` comments + grep-asserts the consumer call IS present AND uncommented, plus a regex pinning the call's position relative to a unique sibling call. Reference: `tests/Feature/Admin6027b9AI788DescriptionsWiredToWizardContractTest.php` for `RadioDeck::make('type')->options(...)->descriptions(...)` chain.
- `MicroweberPackages\Filament\Support\AdminDisplayName::format($state)` is the canonical normaliser for code-identifier columns rendered in admin grids — never roll your own `ucfirst`/`ucwords`/`preg_replace` chain. Reuses the 25-acronym whitelist (AI/SEO/FAQ/URL/API/HTML/RTL/…) so abbreviations stay uppercase across surfaces. Apply via `TextColumn::make('name')->formatStateUsing(fn (?string $state): string => AdminDisplayName::format($state))`.
- **Slice B audit-the-base-class** is the canonical Slice pattern when a defect family appears to recur across sibling resources — read the inheritance chain BEFORE shipping per-resource fixes. AI-816 case: 5 surfaces (CreateContent / EditContent / ContentTableList × 2 / AdminLiveEditPage) all carried the same obsolete green-toolbar-match docblock; one base-class flip in `CreateContent::getHeaderActions()` carried the fix to Post + Page + Product via inheritance. Slice B contract tests include (a) DataProvider over in-scope surfaces, (b) class-inheritance assertions, (c) regression guards for stays-{state} sibling surfaces, (d) AI-NNNa follow-up candidates documented in test docblock for future per-candidate dispatch. Reference: `tests/Feature/Admin2b1020AI816SaveCtaPrimaryColorContractTest.php`.
- **Logo trilogy closed by Path B** (2026-05-17): AI-803 viewport @media gate + AI-803 CHANGE top-level `.logo-link inline-block` parent-flex-collapse fix + AI-804 three-tier alt fallback + AI-805 dead-branch collapse + AI-805a Path B live-edit-disabled navigation (`is_live_edit() ? 'javascript:void(0)' : site_url()` + conditional `title` + `aria-disabled="true"`). 5 commits on one template; `LogoE2e29aAI805DeadBranchCleanupContractTest` carries 3 pin-evolutions in-place (no parallel v2 tests). `javascript:void(0)` chosen over `'#'` because `'#'` jumps to top of page if unhandled; designer's own audit hit the data-loss bug twice in one day.
- **AI-803 CHANGE runtime parent-flex-collapse fix** (`852a8549ba`) formalised the Stage-2 cascade-loss family at 3-recurrence (AI-697 v3 + AI-786 CHANGE + AI-803 CHANGE) — pattern signature "source change correct but a sibling rule (or parent flex context) needed runtime adjustment." Top-level `.logo-module .logo-link { display: inline-block; }` mirrors the same declaration already in the ≤575px block; resolves the `col-xl-4 w-auto` parent-fits-content collapse.
- **Canonical Filament price prefix is `currency_symbol()`** (AI-818, not `option_get('currency_symbol', 'shop')`): shop-default shape `->prefix(fn () => currency_symbol() ?: '$')`; multi-currency wizard shape uses a per-resource `resolveCurrencyPrefix(?string $currency): string` static helper with a 6-currency map (`SubscriptionPlanResource` is the reference). Currency Select must carry `->live()` so the wizard updates the prefix reactively. Never hardcode `'$'`.
- **Filament v5 `--primary-NNN` token override at `:root`** (AI-819, `microweber-theme-v3.scss`): Filament v5's `ColorManager` OKLCH emit is incompatible with project's Tailwind v3 `rgba(var(--primary-500), <alpha-value>)` consumer. Override at `:root` with an RGB-triplet block anchored at `MwColors::Blue` values verbatim (slot 500 = `13, 110, 253` = `#0d6efd`). Bypasses the framework runtime emit; preserves the project's Tailwind v3 consumer shape. New Stage candidate "framework-emit-format-mismatch" added to the taxonomy.
- **AI-817 sr-only label-restoration is the canonical AT-accessible label-hide pattern** (over Filament's `->hiddenLabel()` and over `display: none`): `->label('Title')` + `->extraFieldWrapperAttributes(['class' => 'mw-fb-title-wrap'])` + selector-scoped CSS `position: absolute; width: 1px; height: 1px; overflow: hidden; clip: rect(0,0,0,0);`. AT users read the label; sighted users see the placeholder/affordance only. Class inheritance carries the fix to all sibling resources via `extends CreateContent`.
- **No-pre-reservation policy for Jira ticket numbers** (designer's 2026-05-17 policy after AI-829/AI-830/AI-833 collision chain): never pre-reserve a Jira ticket number in a design brief or SHIP report — Jira auto-assignment can collide with reserved numbers. File the ticket first, accept the auto-assigned number, reference going forward.
- **Pin-evolution discipline** (AI-770 v2 + AI-805/AI-805a Path B): when a spec evolves on the same surface, UPDATE the existing test pin in place with a docblock note pointing at the prior task-ID — never add a parallel test for the new behaviour. Parallel tests create contradictory contracts. Three or more pin-evolutions in one test file is fine; each gets its own note. Reference: `LogoE2e29aAI805DeadBranchCleanupContractTest` (3 pin-evolutions for AI-805a Path B).

## Dependencies (non-obvious)
- `nwidart/laravel-modules` powers the feature-module layout under `Modules/`.
- Filament 5 + Livewire 4 drive the admin UI and many live-edit flows.
- Root frontend asset builds are orchestrated through `run-build.js` and package-level build scripts.
- `packages/frontend-assets/` builds with Vite; `packages/microweber-filament-theme/` builds with Webpack. The two are not interchangeable.
- `packages/frontend-assets/` holds project-authored Vue/SCSS/CSS/JS; `packages/frontend-assets-libs/` is a sibling vendor-bundle for third-party libraries (e.g. `@mdi/font`) built via its own `build.mjs`. Don't confuse them — new application code goes in `frontend-assets`, not `frontend-assets-libs`.
- VitePress (under `docs/`) is the documentation site engine.
- Locale-aware UI relies on `MultilanguageHelpers` + per-locale `ModuleOption` rows — no separate i18n library; admin URL locale switch lives at `/admin/lang/{locale}`.
- The `public` storage disk (`storage/app/public` symlinked to `public/storage`) is the canonical home for uploaded media; `Media::create(['filename' => Storage::disk('public')->url($path), ...])` is the canonical insert shape.

## Credentials
- None recorded in this repository summary by design. Per AGENTS.md §0.1 protocol, any credential the user provides during a session is stored in the Memory MCP server (key shape `credentials/<name>`) and referenced here ONLY by key name + purpose — never the raw value. Query the Memory MCP first before prompting the user for a credential.
- Local dev admin password reset (for Playwright runs against `php artisan serve` when Apache `/admin/login` returns 404): `php artisan tinker --execute='\App\Models\User::where("email","admin@example.com")->first()->update(["password"=>bcrypt("admin")])'`. Used 2026-05-16 / task-f4bf24 to onboard agent-designer. Username/password values are local-dev convention only — never commit to remote.
- `.env` is gitignored; `.env.example` ships placeholder values. PHPUnit reads from `phpunit.xml` env block or `.env.testing`.
