{
    "name": "microweber-packages/security-standalone-validation",
    "description": "Minimal Laravel app validating microweber-security as a standalone package",
    "type": "project",
    "require": {
        "php": "^8.2",
        "microweber-packages/security": "@dev"
    },
    "require-dev": {
        "orchestra/testbench": "^9.0|^10.0",
        "phpunit/phpunit": "^11.0"
    },
    "repositories": [
        {"type": "path", "url": "..", "options": {"symlink": true}}
    ],
    "autoload-dev": {
        "psr-4": {
            "SecurityStandalone\\Tests\\": "tests/"
        }
    },
    "minimum-stability": "dev",
    "prefer-stable": true,
    "config": {
        "allow-plugins": {
            "php-http/discovery": true
        }
    }
}
